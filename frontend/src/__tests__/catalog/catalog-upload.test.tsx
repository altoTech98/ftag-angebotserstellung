import { describe, it } from 'vitest';

describe('[KAT-01] CatalogUpload', () => {
  it.todo('renders upload dropzone for Excel/CSV files');

  it.todo('shows validation results after upload (row count, errors, warnings)');

  it.todo('displays error state for invalid files');

  it.todo('shows upload progress indicator');
});
