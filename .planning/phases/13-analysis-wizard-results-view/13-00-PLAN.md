---
phase: 13-analysis-wizard-results-view
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/src/__tests__/analysis/step-catalog.test.tsx
  - frontend/src/__tests__/analysis/step-config.test.tsx
  - frontend/src/__tests__/analysis/step-progress.test.tsx
  - frontend/src/__tests__/analysis/step-results.test.tsx
  - frontend/src/__tests__/analysis/result-detail.test.tsx
  - frontend/src/__tests__/analysis/comparison-card.test.tsx
  - frontend/src/__tests__/analysis/wizard-init.test.tsx
autonomous: true
requirements: [ANLZ-02, ANLZ-03, ANLZ-04, ANLZ-05, RSLT-01, RSLT-02, RSLT-03]

must_haves:
  truths:
    - "Test stub files exist and can be discovered by Vitest"
    - "Each stub contains at least one skipped or placeholder test describing expected behavior"
    - "Running vitest on the stubs does not crash"
  artifacts:
    - path: "frontend/src/__tests__/analysis/step-catalog.test.tsx"
      provides: "Test stubs for catalog selection step (ANLZ-02)"
    - path: "frontend/src/__tests__/analysis/step-config.test.tsx"
      provides: "Test stubs for config step (ANLZ-03)"
    - path: "frontend/src/__tests__/analysis/step-progress.test.tsx"
      provides: "Test stubs for progress display (ANLZ-04)"
    - path: "frontend/src/__tests__/analysis/step-results.test.tsx"
      provides: "Test stubs for results table (ANLZ-05, RSLT-01, RSLT-04)"
    - path: "frontend/src/__tests__/analysis/result-detail.test.tsx"
      provides: "Test stubs for detail expansion (RSLT-02)"
    - path: "frontend/src/__tests__/analysis/comparison-card.test.tsx"
      provides: "Test stubs for comparison card (RSLT-03)"
    - path: "frontend/src/__tests__/analysis/wizard-init.test.tsx"
      provides: "Test stubs for wizard initialization with analysisId"
  key_links: []
---

<objective>
Create test stub files for all Phase 13 components so that subsequent plans can run behavioral tests alongside type-checking.

Purpose: Satisfies Nyquist Wave 0 requirement -- every plan must have an automated behavioral verify, not just tsc. Stubs establish the test files that plans 01-03 will flesh out as they implement components.
Output: 7 test stub files in `frontend/src/__tests__/analysis/` matching the project's existing test directory convention.
</objective>

<execution_context>
@C:/Users/ALI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ALI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-analysis-wizard-results-view/13-CONTEXT.md
@.planning/phases/13-analysis-wizard-results-view/13-VALIDATION.md

NOTE: Existing tests live in `frontend/src/__tests__/` (e.g., `src/__tests__/auth/login.test.ts`, `src/__tests__/projects/project-crud.test.ts`). The vitest config includes pattern `src/__tests__/**/*.test.ts{x}`. Do NOT place tests alongside components -- follow the established convention.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create analysis test stub files</name>
  <files>
    frontend/src/__tests__/analysis/step-catalog.test.tsx,
    frontend/src/__tests__/analysis/step-config.test.tsx,
    frontend/src/__tests__/analysis/step-progress.test.tsx,
    frontend/src/__tests__/analysis/step-results.test.tsx,
    frontend/src/__tests__/analysis/result-detail.test.tsx,
    frontend/src/__tests__/analysis/comparison-card.test.tsx,
    frontend/src/__tests__/analysis/wizard-init.test.tsx
  </files>
  <action>
    Create the directory `frontend/src/__tests__/analysis/` and 7 test stub files. Each file should:
    - Import `describe`, `it`, `expect` from vitest
    - Contain a `describe` block named after the component
    - Include `it.todo(...)` stubs describing the behaviors that will be tested once components are implemented
    - NOT import the actual components yet (they don't exist yet) -- just describe expected behavior

    Specific stubs per file:

    1. `step-catalog.test.tsx` (ANLZ-02):
       - it.todo('renders default catalog info card')
       - it.todo('shows pre-selected FTAG Produktuebersicht')
       - it.todo('disables upload button with phase 14 tooltip')

    2. `step-config.test.tsx` (ANLZ-03):
       - it.todo('renders high threshold slider with default 90')
       - it.todo('renders low threshold slider with default 70')
       - it.todo('validates lowThreshold < highThreshold')
       - it.todo('renders validation passes selector defaulting to 1')

    3. `step-progress.test.tsx` (ANLZ-04):
       - it.todo('renders 4 stage items: Dokument lesen, Anforderungen extrahieren, Produkte zuordnen, Ergebnis generieren')
       - it.todo('maps SSE progress text to correct active stage')
       - it.todo('shows cancel button with confirmation dialog')
       - it.todo('calls onComplete when analysis succeeds')
       - it.todo('calls onFailed when analysis fails')

    4. `step-results.test.tsx` (ANLZ-05, RSLT-01, RSLT-04):
       - it.todo('renders table with 6 columns: Nr, Anforderung, Position, Zugeordnetes Produkt, Artikelnr, Konfidenz')
       - it.todo('flattens matched + partial + unmatched into single numbered list')
       - it.todo('filters by text search on beschreibung')
       - it.todo('filters by confidence level dropdown')
       - it.todo('sorts by column header click')
       - it.todo('shows Excel download button')
       - it.todo('shows filter summary chips with counts per level')

    5. `result-detail.test.tsx` (RSLT-02):
       - it.todo('renders AI reasoning text from entry.reason')
       - it.todo('renders 6 dimension bars')
       - it.todo('renders comparison card for matched entries')
       - it.todo('renders rejection list for gap entries')

    6. `comparison-card.test.tsx` (RSLT-03):
       - it.todo('renders two-column layout: Anforderung vs Produkt')
       - it.todo('shows field match indicators')
       - it.todo('shows rejection reasons for gap entries')

    7. `wizard-init.test.tsx` (ANLZ-05 -- wizard with pre-loaded result):
       - it.todo('when analysisId is provided, wizard starts at step 5')
       - it.todo('when analysisId is provided, results are loaded from Analysis record')
       - it.todo('when no analysisId, wizard starts at step 1')
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/analysis/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All 7 test files exist in frontend/src/__tests__/analysis/. Running vitest on the directory succeeds (all tests show as todo/skipped). No test failures.</done>
</task>

</tasks>

<verification>
- All 7 test files exist in `frontend/src/__tests__/analysis/`
- `cd frontend && npx vitest run src/__tests__/analysis/` runs without errors
- Test stubs describe the expected behaviors for each component
</verification>

<success_criteria>
- 7 stub files created following project test directory convention
- Vitest discovers and reports all stubs as todo
- Wave 0 complete, subsequent plans can reference these test files
</success_criteria>

<output>
After completion, create `.planning/phases/13-analysis-wizard-results-view/13-00-SUMMARY.md`
</output>
