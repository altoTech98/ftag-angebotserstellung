---
phase: 03-cross-document-intelligence
plan: 03
type: execute
wave: 1
depends_on: ["03-01", "03-02"]
files_modified:
  - backend/v2/extraction/conflict_detector.py
  - backend/tests/test_v2_crossdoc.py
autonomous: true
gap_closure: true
requirements: [DOKA-08]

must_haves:
  truths:
    - "When an Anthropic client is provided, conflicts are resolved via AI call using CROSSDOC_CONFLICT prompts"
    - "resolved_by is set to 'ai' when AI resolves, 'rule' only when client is None or AI call fails"
    - "AI resolution includes domain-aware reasoning (PDF spec > Excel for technical details, safety-conservative)"
    - "Rule-based fallback still works when client is None"
  artifacts:
    - path: "backend/v2/extraction/conflict_detector.py"
      provides: "AI-powered conflict resolution with rule-based fallback"
      contains: "asyncio.to_thread"
    - path: "backend/tests/test_v2_crossdoc.py"
      provides: "Tests for AI resolution path and rule fallback"
      contains: "test_ai_resolution"
  key_links:
    - from: "backend/v2/extraction/conflict_detector.py"
      to: "backend/v2/extraction/prompts.py"
      via: "import CROSSDOC_CONFLICT_SYSTEM_PROMPT, CROSSDOC_CONFLICT_USER_TEMPLATE"
      pattern: "CROSSDOC_CONFLICT"
    - from: "backend/v2/extraction/conflict_detector.py"
      to: "anthropic client"
      via: "asyncio.to_thread(client.messages.parse, ...)"
      pattern: "asyncio\\.to_thread"
---

<objective>
Wire AI-powered conflict resolution in conflict_detector.py to actually call Claude via the Anthropic client when provided.

Purpose: The verification found that `_resolve_conflicts_with_ai()` accepts a client parameter but never calls it -- all conflicts are resolved rule-based. This gap closure implements the actual AI call using the existing CROSSDOC_CONFLICT prompts, with 3x retry and rule-based fallback when client is None or AI fails.

Output: Working AI conflict resolution with transparent reasoning, rule-based fallback preserved.
</objective>

<execution_context>
@C:/Users/ALI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ALI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cross-document-intelligence/03-01-SUMMARY.md
@.planning/phases/03-cross-document-intelligence/03-02-SUMMARY.md

<interfaces>
<!-- Existing prompts in backend/v2/extraction/prompts.py -->
CROSSDOC_CONFLICT_SYSTEM_PROMPT  -- German prompt for conflict analysis (document type priority, domain logic, safety-conservative)
CROSSDOC_CONFLICT_USER_TEMPLATE  -- Template with {conflicts_json} placeholder, expects JSON list with resolution + resolution_reason per conflict

<!-- Existing schema in backend/v2/schemas/extraction.py -->
class FieldConflict(BaseModel):
    positions_nr: str
    field_name: str
    wert_a: str
    quelle_a: FieldSource
    wert_b: str
    quelle_b: FieldSource
    severity: ConflictSeverity
    resolution: str
    resolution_reason: str
    resolved_by: str  # "ai" or "rule"

<!-- Established async AI call pattern from pass3_validation.py -->
response = await asyncio.to_thread(
    client.messages.parse,
    model="claude-sonnet-4-20250514",
    max_tokens=16384,
    system=SYSTEM_PROMPT,
    messages=messages,
    output_format=PydanticModel,
)

<!-- Retry pattern from pass3_validation.py -->
_MAX_RETRIES = 3
_BASE_BACKOFF_SECONDS = 2
for attempt in range(_MAX_RETRIES):
    try:
        result = await call(...)
        return result
    except Exception as e:
        backoff = _BASE_BACKOFF_SECONDS ** (attempt + 1)
        if attempt < _MAX_RETRIES - 1:
            await asyncio.sleep(backoff)
# Fallback on exhaustion
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement AI conflict resolution with retry and fallback</name>
  <files>backend/v2/extraction/conflict_detector.py, backend/tests/test_v2_crossdoc.py</files>
  <behavior>
    - Test: When client is provided and AI call succeeds, resolved_by is "ai" and resolution_reason contains AI reasoning
    - Test: When client is None, falls back to rule-based resolution with resolved_by="rule"
    - Test: When AI call raises exception 3 times, falls back to rule-based with resolved_by="rule" and a warning logged
    - Test: AI receives conflict data formatted via CROSSDOC_CONFLICT_USER_TEMPLATE
    - Test: Existing rule-based tests (patching _resolve_conflicts_with_ai) still pass unchanged
  </behavior>
  <action>
1. Create a Pydantic response model for AI conflict resolution output:
   ```python
   class ConflictResolutionItem(BaseModel):
       """AI response for a single conflict resolution."""
       field_name: str
       resolution: str
       resolution_reason: str

   class ConflictResolutionResult(BaseModel):
       """Structured output from AI conflict resolution."""
       resolutions: list[ConflictResolutionItem]
   ```
   Place this in conflict_detector.py (internal to the module, not in schemas).

2. Make `_resolve_conflicts_with_ai()` async and implement the actual AI call:
   - Import `asyncio`, `json`, `anthropic` (for type hint), and `CROSSDOC_CONFLICT_SYSTEM_PROMPT`, `CROSSDOC_CONFLICT_USER_TEMPLATE` from prompts
   - When `client is not None`:
     a. Format raw_conflicts as JSON for the prompt using CROSSDOC_CONFLICT_USER_TEMPLATE
     b. Call `asyncio.to_thread(client.messages.parse, ...)` with `output_format=ConflictResolutionResult`
     c. Map AI resolutions back to raw_conflicts by field_name matching
     d. Set `resolved_by="ai"` for AI-resolved conflicts
     e. 3x retry with exponential backoff (base=2s) following pass3_validation.py pattern
     f. On all retries exhausted, log warning and fall through to rule-based resolution
   - When `client is None`: use existing rule-based logic (higher confidence wins), set `resolved_by="rule"`

3. Update `detect_and_resolve_conflicts()` to be async (add `async def`) and `await` the now-async `_resolve_conflicts_with_ai()`.

4. Add tests in test_v2_crossdoc.py:
   - `test_ai_resolution_called_when_client_provided`: Mock client.messages.parse to return ConflictResolutionResult, verify resolved_by="ai"
   - `test_rule_fallback_when_no_client`: Pass client=None, verify resolved_by="rule"
   - `test_rule_fallback_on_ai_failure`: Mock client.messages.parse to raise Exception 3 times, verify resolved_by="rule"
   - Use `unittest.mock.patch` and `unittest.mock.AsyncMock` / `MagicMock` as appropriate

5. Update the call site in pipeline.py: the existing call to `detect_and_resolve_conflicts()` should already be awaited (check and fix if needed since it was sync before).
  </action>
  <verify>
    <automated>cd C:/Users/ALI/Desktop/ClaudeCodeTest/backend && python -m pytest tests/test_v2_crossdoc.py -x -v 2>&1 | tail -40</automated>
  </verify>
  <done>
    - _resolve_conflicts_with_ai() calls Claude via asyncio.to_thread when client is provided
    - resolved_by="ai" when AI succeeds, "rule" when client is None or AI fails after 3 retries
    - All existing tests pass (no regressions)
    - New tests cover AI path, no-client fallback, and retry-exhaustion fallback
  </done>
</task>

</tasks>

<verification>
- `cd backend && python -m pytest tests/test_v2_crossdoc.py -x -v` -- all tests pass including new AI resolution tests
- `cd backend && python -m pytest tests/ -x` -- full test suite green (no regressions)
- Grep `asyncio.to_thread` in conflict_detector.py confirms AI call is wired
- Grep `resolved_by.*ai` in conflict_detector.py confirms AI resolution path sets correct value
- Grep `CROSSDOC_CONFLICT` in conflict_detector.py confirms prompt imports are used
</verification>

<success_criteria>
- conflict_detector.py calls Claude API via asyncio.to_thread when client is provided
- AI resolution uses CROSSDOC_CONFLICT_SYSTEM_PROMPT and CROSSDOC_CONFLICT_USER_TEMPLATE
- 3x retry with exponential backoff on AI failure, then rule-based fallback
- resolved_by field correctly distinguishes "ai" vs "rule" resolution
- All 31+ existing tests pass, new tests cover the AI path
</success_criteria>

<output>
After completion, create `.planning/phases/03-cross-document-intelligence/03-03-SUMMARY.md`
</output>
