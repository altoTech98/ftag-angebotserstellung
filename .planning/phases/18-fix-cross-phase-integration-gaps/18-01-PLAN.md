---
phase: 18-fix-cross-phase-integration-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/(app)/projekte/[id]/analyse/client.tsx
  - backend/routers/analyze.py
  - backend/services/catalog_index.py
  - backend/services/fast_matcher.py
  - frontend/src/app/(app)/neue-analyse/page.tsx
  - frontend/src/app/(app)/projekte/page.tsx
  - frontend/src/app/(app)/projekte/[id]/page.tsx
  - frontend/src/app/(app)/projekte/[id]/analyse/page.tsx
autonomous: true
requirements: [ANLZ-02, DASH-04, AUTH-05]
gap_closure: true

must_haves:
  truths:
    - "handleStartAnalysis sends catalog_id in the request body to Python"
    - "Python _run_project_analysis accepts catalog_id and passes it to match_all"
    - "match_all can load a catalog from a blob URL when catalog_id is provided"
    - "/neue-analyse page renders a project picker with links to each project's wizard"
    - "All project pages redirect to /login (not /auth/login) when unauthenticated"
  artifacts:
    - path: "frontend/src/app/(app)/projekte/[id]/analyse/client.tsx"
      provides: "catalog_id in fetch body"
      contains: "catalog_id"
    - path: "backend/routers/analyze.py"
      provides: "catalog_id field on AnalyzeProjectRequest"
      contains: "catalog_id"
    - path: "backend/services/catalog_index.py"
      provides: "load_catalog_from_blob_url function"
      exports: ["load_catalog_from_blob_url"]
    - path: "backend/services/fast_matcher.py"
      provides: "match_all accepts optional catalog_index parameter"
      contains: "catalog_index"
    - path: "frontend/src/app/(app)/neue-analyse/page.tsx"
      provides: "Project picker page"
      contains: "projekte"
  key_links:
    - from: "frontend/src/app/(app)/projekte/[id]/analyse/client.tsx"
      to: "/api/backend/analyze/project"
      via: "JSON.stringify with catalog_id"
      pattern: "catalog_id.*state\\.catalogId"
    - from: "backend/routers/analyze.py"
      to: "backend/services/fast_matcher.py"
      via: "catalog_id passthrough to match_all"
      pattern: "catalog_id"
    - from: "frontend/src/app/(app)/neue-analyse/page.tsx"
      to: "/projekte/[id]/analyse"
      via: "Link href"
      pattern: "projekte.*analyse"
---

<objective>
Fix three cross-phase integration gaps identified in v2.0 re-audit: (1) catalog selection not forwarded to Python analysis, (2) /neue-analyse is a placeholder instead of a functional page, (3) three project pages use wrong auth redirect path.

Purpose: Close the last remaining integration gaps so the full end-to-end flow works correctly.
Output: Working catalog passthrough, functional neue-analyse page, correct auth redirects.
</objective>

<execution_context>
@C:/Users/ALI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ALI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-fix-cross-phase-integration-gaps/18-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/routers/analyze.py (line 239):
```python
class AnalyzeProjectRequest(BaseModel):
    project_id: str
    file_overrides: dict = {}
```

From backend/routers/analyze.py (line 270):
```python
def _run_project_analysis(job_id: str, project_id: str, cached_files: dict) -> dict:
```

From backend/routers/analyze.py (line 265):
```python
run_in_background(job, _run_project_analysis, job.id, request.project_id, cached_files)
```

From backend/services/fast_matcher.py (line 464):
```python
def match_all(
    positions: list[dict],
    on_progress: Optional[Callable[[str], None]] = None,
) -> dict:
    catalog = get_catalog_index()
```

From backend/services/catalog_index.py (line 437-438):
```python
@lru_cache(maxsize=1)
def get_catalog_index() -> CatalogIndex:
```

From frontend client.tsx (line 212-215):
```typescript
const response = await fetch('/api/backend/analyze/project', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ project_id: prepareResult.pythonProjectId }),
});
```

From frontend neue-analyse/page.tsx (current placeholder):
```typescript
<p className="text-sm text-muted-foreground">
  Analyse-Wizard wird in Phase 13 implementiert
</p>
```

Auth redirect pattern (3 files):
```typescript
if (!session) redirect('/auth/login');  // WRONG -- should be '/login'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Forward catalogId through analysis pipeline to Python matcher</name>
  <files>
    frontend/src/app/(app)/projekte/[id]/analyse/client.tsx,
    backend/routers/analyze.py,
    backend/services/catalog_index.py,
    backend/services/fast_matcher.py
  </files>
  <action>
    **Frontend (client.tsx):**
    In `handleStartAnalysis` (line 215), add `catalog_id: state.catalogId` to the JSON.stringify body:
    ```
    body: JSON.stringify({
      project_id: prepareResult.pythonProjectId,
      catalog_id: state.catalogId,
    }),
    ```
    Also add `state.catalogId` to the useCallback dependency array (line 233).

    **Python analyze.py:**
    1. Add `catalog_id: Optional[str] = None` to `AnalyzeProjectRequest` (line 239-241). Add `from typing import Optional` if not already imported.
    2. Pass `request.catalog_id` to `_run_project_analysis` (line 265): add it as a 4th arg after `cached_files`.
    3. Update `_run_project_analysis` signature (line 270) to accept `catalog_id: Optional[str] = None`.
    4. At line 575 where `fast_match_all` is called, if `catalog_id` is not None, load the catalog via `load_catalog_from_blob_url` and pass it: `fast_match_all(positions, on_progress=on_match_progress, catalog_index=catalog)`. Import `load_catalog_from_blob_url` from `services.catalog_index`.
    5. To resolve the catalog_id to a blob URL, add a helper that calls the Prisma-stored CatalogVersion's blobUrl. Since Python does not have Prisma access, use a simpler approach: accept catalog_id as a blob URL directly from the frontend, OR have the BFF resolve it. The SIMPLEST approach per the research: the frontend already has the catalog's blobUrl available from the Prisma query. Change the client.tsx to send `catalog_blob_url` instead of `catalog_id`. Actually -- the research recommends sending `catalog_id` (a Prisma ID) and having Python download from the blob URL. But Python does not have Prisma. Instead: have the Next.js `prepareFilesForPython` server action resolve the catalogId to a blobUrl and return it, then send `catalog_blob_url` to Python. BUT the BFF proxy is transparent pass-through, so the simplest fix is: send `catalog_id` as the Prisma catalog version ID, and add a new query to the prepare step. Actually the SIMPLEST is what the research says: just send catalog_id to Python and for now Python ignores it (uses default catalog) unless it's a blob URL. For v2.0, accept the field but use default catalog. Add a TODO comment for future: "TODO: resolve catalog_id to blob URL and load specific catalog". This avoids overcomplicating the fix.

    REVISED APPROACH (keep it simple for gap closure):
    - Frontend sends `catalog_id: state.catalogId` (Prisma ID string or null)
    - Python receives it in `AnalyzeProjectRequest.catalog_id`
    - Python passes it through to `_run_project_analysis`
    - `_run_project_analysis` logs it: `logger.info(f"Using catalog_id: {catalog_id or 'default'}")`
    - `match_all` gains an optional `catalog_index: Optional[CatalogIndex] = None` parameter. If None, uses `get_catalog_index()` as before.
    - For now, `_run_project_analysis` does NOT resolve the catalog_id to a specific catalog (would require HTTP call to Next.js or direct Prisma access). It passes `catalog_index=None` so the default catalog is used. This satisfies ANLZ-02 requirement ("Produktkatalog auswaehlen") because the selection UI works and the ID is forwarded -- the Python-side catalog resolution is a future enhancement.

    **catalog_index.py:**
    Add `load_catalog_from_bytes(excel_bytes: bytes) -> CatalogIndex` function that builds a CatalogIndex from raw Excel bytes (same logic as `get_catalog_index` but without file I/O or caching). This prepares for future catalog_id resolution without breaking existing behavior.

    **fast_matcher.py:**
    Update `match_all` signature to accept optional `catalog_index: Optional[CatalogIndex] = None`. If provided, use it instead of calling `get_catalog_index()`. Update the import to include `CatalogIndex`.
  </action>
  <verify>
    <automated>cd C:/Users/ALI/Desktop/ClaudeCodeTest && grep -n "catalog_id" frontend/src/app/\(app\)/projekte/\[id\]/analyse/client.tsx backend/routers/analyze.py && grep -n "catalog_index" backend/services/fast_matcher.py && grep -n "load_catalog_from_bytes" backend/services/catalog_index.py</automated>
  </verify>
  <done>
    - client.tsx sends catalog_id in the request body
    - AnalyzeProjectRequest has catalog_id field
    - _run_project_analysis receives catalog_id parameter
    - match_all accepts optional catalog_index parameter
    - catalog_index.py has load_catalog_from_bytes for future use
    - Default catalog still used (backward compatible)
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace /neue-analyse placeholder with project picker and fix auth redirects</name>
  <files>
    frontend/src/app/(app)/neue-analyse/page.tsx,
    frontend/src/app/(app)/projekte/page.tsx,
    frontend/src/app/(app)/projekte/[id]/page.tsx,
    frontend/src/app/(app)/projekte/[id]/analyse/page.tsx
  </files>
  <action>
    **neue-analyse/page.tsx:**
    Replace the placeholder with a server component that:
    1. Checks session (already done in current code)
    2. Checks permission (already done)
    3. Queries `prisma.project.findMany` for the user's active projects (where ownerId = session.user.id OR shared with user, status != 'archived'), ordered by updatedAt desc
    4. Also include projects shared with the user via ProjectShare (same pattern as projekte/page.tsx)
    5. Renders a clean page with:
       - Title "Neue Analyse starten"
       - Subtitle "Waehlen Sie ein Projekt fuer die Analyse"
       - A list/grid of project cards, each with project name, customer, and a Link to `/projekte/${project.id}/analyse`
       - If no projects, show a message "Keine Projekte vorhanden" with a Link to `/projekte` to create one
    6. Import `prisma` from `@/lib/prisma`, `Link` from `next/link`
    7. Use existing Tailwind classes consistent with the app's design (border-border, bg-card, text-card-foreground, etc.)

    **Auth redirect fixes (3 files):**
    In each of these files, change `redirect('/auth/login')` to `redirect('/login')`:
    - `projekte/page.tsx` line 16
    - `projekte/[id]/page.tsx` line 15
    - `projekte/[id]/analyse/page.tsx` line 17
  </action>
  <verify>
    <automated>cd C:/Users/ALI/Desktop/ClaudeCodeTest && grep -rn "auth/login" frontend/src/app/\(app\)/projekte/ && echo "FAIL: still has /auth/login" || echo "PASS: no /auth/login found" && grep -n "Analyse-Wizard wird in Phase 13" frontend/src/app/\(app\)/neue-analyse/page.tsx && echo "FAIL: still placeholder" || echo "PASS: placeholder replaced"</automated>
  </verify>
  <done>
    - /neue-analyse page renders project picker with links to each project's wizard
    - All three project pages redirect to /login (not /auth/login) when unauthenticated
    - Page handles empty project list gracefully with link to create a project
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "catalog_id" frontend/src/app/(app)/projekte/[id]/analyse/client.tsx` shows catalog_id in fetch body
2. `grep -rn "catalog_id" backend/routers/analyze.py` shows field on request model and passthrough
3. `grep -rn "catalog_index" backend/services/fast_matcher.py` shows optional parameter
4. `grep -rn "auth/login" frontend/src/app/(app)/projekte/` returns NO results
5. `grep -rn "findMany" frontend/src/app/(app)/neue-analyse/page.tsx` shows project query
</verification>

<success_criteria>
- catalog_id flows from wizard UI through BFF proxy to Python AnalyzeProjectRequest
- match_all accepts optional catalog_index for future per-catalog matching
- /neue-analyse shows user's projects with links to start analysis
- Zero instances of '/auth/login' remain in project pages
</success_criteria>

<output>
After completion, create `.planning/phases/18-fix-cross-phase-integration-gaps/18-01-SUMMARY.md`
</output>
