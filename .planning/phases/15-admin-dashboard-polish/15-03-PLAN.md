---
phase: 15-admin-dashboard-polish
plan: "03"
type: execute
wave: 3
depends_on: ["15-01", "15-02"]
files_modified:
  - frontend/src/emails/password-reset.tsx
  - frontend/src/emails/user-invitation.tsx
  - frontend/src/emails/analysis-complete.tsx
  - frontend/src/lib/auth.ts
  - frontend/src/lib/actions/admin-actions.ts
  - frontend/src/lib/actions/analysis-actions.ts
  - frontend/src/components/keyboard-shortcuts/shortcut-provider.tsx
  - frontend/src/components/keyboard-shortcuts/shortcuts-help.tsx
  - frontend/src/app/(app)/layout.tsx
  - frontend/src/app/(app)/dashboard/loading.tsx
  - frontend/src/app/(app)/projekte/loading.tsx
  - frontend/src/app/(app)/admin/loading.tsx
autonomous: true
requirements:
  - INFRA-05
  - UI-05
  - UI-06

must_haves:
  truths:
    - "Password reset sends a real email with OTP code via Resend"
    - "User invitation sends a branded email with signup link"
    - "Analysis completion triggers an email notification to the user who started it"
    - "Pressing N navigates to new analysis, D to dashboard, P to projects, K to catalog"
    - "Pressing ? opens a keyboard shortcuts help dialog"
    - "Keyboard shortcuts do not fire when typing in input fields"
    - "Dashboard, projects, and admin pages show skeleton loaders during loading"
  artifacts:
    - path: "frontend/src/emails/password-reset.tsx"
      provides: "Password reset email template"
      min_lines: 30
    - path: "frontend/src/emails/user-invitation.tsx"
      provides: "User invitation email template"
      min_lines: 30
    - path: "frontend/src/emails/analysis-complete.tsx"
      provides: "Analysis completion email template"
      min_lines: 30
    - path: "frontend/src/components/keyboard-shortcuts/shortcut-provider.tsx"
      provides: "Global keyboard shortcut handler"
      min_lines: 20
    - path: "frontend/src/components/keyboard-shortcuts/shortcuts-help.tsx"
      provides: "Shortcuts help modal"
      min_lines: 30
  key_links:
    - from: "frontend/src/lib/auth.ts"
      to: "frontend/src/emails/password-reset.tsx"
      via: "emailOTP sendVerificationOTP calling Resend"
      pattern: "resend\\.emails\\.send"
    - from: "frontend/src/lib/actions/admin-actions.ts"
      to: "frontend/src/emails/user-invitation.tsx"
      via: "inviteUser calling Resend after user creation"
      pattern: "resend\\.emails\\.send"
    - from: "frontend/src/app/(app)/layout.tsx"
      to: "frontend/src/components/keyboard-shortcuts/shortcut-provider.tsx"
      via: "ShortcutProvider wrapping layout children"
      pattern: "ShortcutProvider"
---

<objective>
Email notifications (Resend + React Email), keyboard shortcuts with help dialog, and skeleton loaders for all main pages.

Purpose: Completes the UX polish layer -- real email delivery replaces console.log placeholders, power users get keyboard navigation, and all pages feel fast with skeleton loading states.
Output: 3 email templates wired to Resend, global keyboard shortcuts with ? help, skeleton loading.tsx files for main routes.
</objective>

<execution_context>
@C:/Users/ALI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ALI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-admin-dashboard-polish/15-00-SUMMARY.md
@.planning/phases/15-admin-dashboard-polish/15-01-SUMMARY.md
@.planning/phases/15-admin-dashboard-polish/15-02-SUMMARY.md
@frontend/src/lib/auth.ts
@frontend/src/lib/email.ts
@frontend/src/lib/actions/admin-actions.ts
@frontend/src/app/(app)/layout.tsx

<interfaces>
From frontend/src/lib/email.ts (created in 15-00):
```typescript
import { Resend } from 'resend';
export const resend: Resend;
export const EMAIL_FROM: string;
```

From frontend/src/lib/auth.ts (emailOTP plugin):
```typescript
emailOTP({
  async sendVerificationOTP({ email, otp, type }) {
    // Currently: console.log placeholder
    // Replace with Resend email send
  },
})
```

From frontend/src/lib/hooks/use-keyboard-shortcuts.ts (created in 15-00):
```typescript
export function useKeyboardShortcuts(): void;
// Handles: n->/neue-analyse, d->/dashboard, p->/projekte, k->/katalog, ?->help
```

From frontend/src/components/ui/skeleton.tsx (created in 15-00):
```typescript
export function Skeleton({ className, ...props }: React.HTMLAttributes<HTMLDivElement>): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: React Email templates + Resend wiring</name>
  <files>
    frontend/src/emails/password-reset.tsx,
    frontend/src/emails/user-invitation.tsx,
    frontend/src/emails/analysis-complete.tsx,
    frontend/src/lib/auth.ts,
    frontend/src/lib/actions/admin-actions.ts,
    frontend/src/lib/actions/analysis-actions.ts
  </files>
  <action>
Per user decision: Minimal branded design -- FTAG logo text, red accent line, white background, gray footer. German language throughout.

All three templates share a common layout structure:
- White background, max-width 480px, centered
- Top: "FTAG Angebotserstellung" text (bold, not an image logo) + red Hr (borderColor '#dc2626', borderWidth 2px)
- Content section
- Footer: gray text (color '#6b7280', fontSize 12px) with "Frank Tueren AG, Buochs NW"
- Use @react-email/components: Html, Head, Body, Container, Text, Section, Hr, Button, Link

1. Create `frontend/src/emails/password-reset.tsx`:
   - Props: `{ otp: string, name: string }`
   - Heading: "Passwort zuruecksetzen"
   - Body: "Hallo {name}, Ihr Verifizierungscode lautet:"
   - OTP display: centered Section with large bold text (fontSize 32px, letterSpacing 4px) on light gray bg (#f3f4f6)
   - Note: "Dieser Code ist 10 Minuten gueltig."

2. Create `frontend/src/emails/user-invitation.tsx`:
   - Props: `{ name: string, invitedBy: string, loginUrl: string }`
   - Heading: "Sie wurden eingeladen"
   - Body: "Hallo {name}, {invitedBy} hat Sie zur FTAG Angebotserstellung eingeladen."
   - Text: "Sie koennen sich ab sofort anmelden und Ihr Passwort bei der ersten Anmeldung aendern."
   - Button: "Jetzt anmelden" linking to loginUrl (background '#dc2626', color white, padding, borderRadius)

3. Create `frontend/src/emails/analysis-complete.tsx`:
   - Props: `{ userName: string, projectName: string, matchCount: number, gapCount: number, avgConfidence: number, resultUrl: string }`
   - Heading: "Analyse abgeschlossen"
   - Body: "Hallo {userName}, die Analyse fuer Projekt '{projectName}' wurde abgeschlossen."
   - Stats section: three-row mini table -- "Matches: {matchCount}", "Gaps: {gapCount}", "Konfidenz: {avgConfidence}%"
   - Button: "Ergebnisse ansehen" linking to resultUrl

4. Wire password reset into `frontend/src/lib/auth.ts`:
   - Import { resend, EMAIL_FROM } from '@/lib/email'
   - Import { PasswordResetEmail } from '@/emails/password-reset'
   - Replace console.log placeholder in emailOTP.sendVerificationOTP with:
     ```
     const user = await prisma.user.findUnique({ where: { email } });
     await resend.emails.send({
       from: EMAIL_FROM,
       to: [email],
       subject: 'Ihr Verifizierungscode',
       react: PasswordResetEmail({ otp, name: user?.name || email }),
     });
     ```
   - Import prisma from '@/lib/prisma'
   - Wrap in try/catch: log error but do not throw (OTP should still work even if email fails)

5. Wire user invitation into `frontend/src/lib/actions/admin-actions.ts`:
   - Import { resend, EMAIL_FROM } from '@/lib/email'
   - Import { UserInvitationEmail } from '@/emails/user-invitation'
   - In inviteUser action, after creating user, send invitation email:
     ```
     await resend.emails.send({
       from: EMAIL_FROM,
       to: [email],
       subject: 'Einladung zur FTAG Angebotserstellung',
       react: UserInvitationEmail({ name, invitedBy: session.user.name, loginUrl: `${process.env.BETTER_AUTH_URL}/login` }),
     });
     ```
   - Wrap in try/catch: log error, do not fail the invite action

6. Wire analysis completion into `frontend/src/lib/actions/analysis-actions.ts`:
   - Add a new exported function `sendAnalysisCompleteEmail(analysisId: string)`:
     - Fetch analysis with project and user relations
     - Parse result JSON for match/gap counts and avg confidence
     - Send email via Resend with AnalysisCompleteEmail template
     - Wrap in try/catch
   - This function should be called from the SSE completion handler or wherever analysis status changes to 'completed'
   - If no obvious hook point exists, add a comment marking where to call it: `// TODO: Call sendAnalysisCompleteEmail(analysisId) when analysis completes`
  </action>
  <verify>
    <automated>cd C:/Users/ALI/Desktop/ClaudeCodeTest/frontend && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Three React Email templates created with FTAG branding, Resend wired into auth.ts for password reset OTP, admin-actions.ts for user invitation, and analysis-actions.ts for completion notification</done>
</task>

<task type="auto">
  <name>Task 2: Keyboard shortcuts + skeleton loaders</name>
  <files>
    frontend/src/components/keyboard-shortcuts/shortcut-provider.tsx,
    frontend/src/components/keyboard-shortcuts/shortcuts-help.tsx,
    frontend/src/app/(app)/layout.tsx,
    frontend/src/app/(app)/dashboard/loading.tsx,
    frontend/src/app/(app)/projekte/loading.tsx,
    frontend/src/app/(app)/admin/loading.tsx,
    frontend/src/app/(app)/katalog/loading.tsx
  </files>
  <action>
1. Create `frontend/src/components/keyboard-shortcuts/shortcut-provider.tsx` ('use client'):
   - Import useKeyboardShortcuts from '@/lib/hooks/use-keyboard-shortcuts'
   - Import ShortcutsHelp dialog component
   - Manages open/close state for help dialog (listens to 'toggle-shortcuts-help' custom event)
   - Calls useKeyboardShortcuts() hook
   - Renders children + ShortcutsHelp dialog
   - Props: `{ children: React.ReactNode }`

2. Create `frontend/src/components/keyboard-shortcuts/shortcuts-help.tsx` ('use client'):
   - shadcn Dialog showing available shortcuts
   - Title: "Tastenkuerzel"
   - Table or list layout:
     - N -> "Neue Analyse starten"
     - D -> "Dashboard"
     - P -> "Projekte"
     - K -> "Katalog"
     - ? -> "Diese Hilfe anzeigen"
   - Each shortcut row: key in a kbd-styled box (inline-flex bg-muted rounded px-2 py-1 font-mono text-sm) + description
   - Props: `{ open: boolean, onOpenChange: (open: boolean) => void }`

3. Update `frontend/src/app/(app)/layout.tsx`:
   - Import ShortcutProvider
   - Wrap the existing layout children with `<ShortcutProvider>{children}</ShortcutProvider>`
   - Place inside the existing AppShell/AppShellClient wrapper (after auth context, so shortcuts only active when logged in)

4. Create skeleton loading.tsx files for main routes. Each uses the Skeleton component from ui/skeleton.tsx to match the page layout shape:

   `dashboard/loading.tsx`:
   - Header skeleton: h-8 w-48 (title) + h-10 w-40 float-right (button)
   - 4 stat card skeletons in grid: each Card with h-4 w-24 (label) + h-8 w-16 (number)
   - Two-column layout skeleton: left Card with 5x h-4 lines, right Card with h-4 lines + h-6 bar

   `projekte/loading.tsx`:
   - Header skeleton: h-8 w-48 + h-10 w-32 (create button)
   - 3 project card skeletons: each with h-5 w-48 (title) + h-4 w-32 (customer) + h-3 w-24 (date)

   `admin/loading.tsx`:
   - Header skeleton: h-8 w-48
   - Tab bar skeleton: 3x h-8 w-24 inline
   - Table skeleton: 5 rows of h-4 across 5 columns

   `katalog/loading.tsx`:
   - Header skeleton: h-8 w-48 + h-10 w-32 (upload button)
   - 2 catalog card skeletons: each with h-5 w-48 + h-4 w-32 + h-3 w-24

   All loading.tsx files are server components (no 'use client'), export default function, use Skeleton + Card components.
  </action>
  <verify>
    <automated>cd C:/Users/ALI/Desktop/ClaudeCodeTest/frontend && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Keyboard shortcuts work globally (N/D/P/K/?), help dialog shows all shortcuts, skeleton loaders appear on dashboard/projects/admin/catalog pages during loading</done>
</task>

</tasks>

<verification>
- All 3 email templates render valid HTML (importable without errors)
- auth.ts sendVerificationOTP calls Resend instead of console.log
- Keyboard shortcuts navigate correctly (manual check)
- ? key opens shortcuts help dialog
- loading.tsx files exist for dashboard, projekte, admin, katalog
- All existing tests pass
</verification>

<success_criteria>
- Password reset OTP emails sent via Resend with FTAG-branded template
- User invitation emails sent on invite with "Jetzt anmelden" button
- Analysis completion email function exists and is callable
- N/D/P/K/? keyboard shortcuts work from any non-input context
- Shortcuts help dialog displays all available shortcuts
- Dashboard, projects, admin, and catalog pages show skeleton loaders during Next.js Suspense loading
</success_criteria>

<output>
After completion, create `.planning/phases/15-admin-dashboard-polish/15-03-SUMMARY.md`
</output>
