---
phase: 10-foundation
plan: 04
type: execute
wave: 3
depends_on: ["10-02", "10-03"]
files_modified:
  - frontend/src/app/(app)/layout.tsx
  - frontend/src/app/page.tsx
  - frontend/src/app/(auth)/login/page.tsx
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-05
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "Session warning modal is wired into the authenticated layout"
    - "Root page redirects to /dashboard (authenticated) or /login (unauthenticated)"
    - "Full auth flow works end-to-end: login -> dashboard -> sidebar nav -> logout"
    - "Responsive layout adapts correctly at desktop, tablet, and mobile breakpoints"
  artifacts:
    - path: "frontend/src/app/(app)/layout.tsx"
      provides: "Authenticated layout with session warning modal integrated"
      contains: "SessionWarningModal"
    - path: "frontend/src/app/page.tsx"
      provides: "Root redirect to dashboard or login"
  key_links:
    - from: "frontend/src/app/(app)/layout.tsx"
      to: "frontend/src/components/auth/session-warning-modal.tsx"
      via: "component import and render"
      pattern: "SessionWarningModal"
    - from: "frontend/src/app/(app)/layout.tsx"
      to: "frontend/src/hooks/use-session-timeout.ts"
      via: "hook usage for idle detection"
      pattern: "useSessionTimeout"
---

<objective>
Wire session timeout into the app layout, finalize root redirect, and verify the complete integrated experience.

Purpose: Connect auth (Plan 02) with layout (Plan 03) so session warnings work inside the authenticated shell. Verify the full end-to-end flow visually.
Output: Fully integrated foundation with auth + layout + session management.
</objective>

<execution_context>
@C:/Users/ALI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ALI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-foundation/10-CONTEXT.md
@.planning/phases/10-foundation/10-02-SUMMARY.md
@.planning/phases/10-foundation/10-03-SUMMARY.md

<interfaces>
<!-- From Plan 02 outputs -->
From src/hooks/use-session-timeout.ts:
```typescript
export function useSessionTimeout(expiresIn: number): {
  showWarning: boolean;
  extendSession: () => Promise<void>;
  remainingTime: number;
}
```

From src/components/auth/session-warning-modal.tsx:
```typescript
// Props: { showWarning: boolean, extendSession: () => Promise<void>, remainingTime: number, onLogout: () => void }
```

<!-- From Plan 03 outputs -->
From src/app/(app)/layout.tsx:
```typescript
// Server component with session check, renders Sidebar + Header + children
// Needs client wrapper for session timeout hook
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire session warning into app layout and finalize root redirect</name>
  <files>
    frontend/src/app/(app)/layout.tsx,
    frontend/src/app/page.tsx,
    frontend/src/app/(auth)/login/page.tsx
  </files>
  <action>
**Session Warning Integration:**
1. The (app)/layout.tsx from Plan 03 is a server component (for session checking). The session timeout hook is a client hook. Solution: create a small client wrapper component inside the layout file or as a separate file.

   Create a client component `AppShellClient` (can be inline in layout or separate file `src/components/layout/app-shell-client.tsx`):
   - "use client"
   - Uses `useSessionTimeout(28800000)` (8 hours in ms)
   - Renders `<SessionWarningModal showWarning={showWarning} extendSession={extendSession} remainingTime={remainingTime} onLogout={() => router.push("/login")} />`
   - Renders {children} passed through

   Update (app)/layout.tsx to wrap children with this client component:
   ```tsx
   <AppShellClient>
     <Sidebar user={session.user} />
     <main>
       <Header user={session.user} />
       {children}
     </main>
   </AppShellClient>
   ```
   Or alternatively place SessionWarningModal as a sibling (not wrapping) -- either approach works as long as the modal appears on all authenticated pages.

**Root Redirect:**
2. Update `src/app/page.tsx`:
   - Server component
   - Check session via `auth.api.getSession({ headers: await headers() })`
   - If authenticated: `redirect("/dashboard")`
   - If not authenticated: `redirect("/login")`

**Login Redirect for Authenticated Users:**
3. Update `src/app/(auth)/login/page.tsx`:
   - Before rendering login form, check if user already has a session
   - If already authenticated: `redirect("/dashboard")` (don't show login page to logged-in users)

**Final Verification Build:**
4. Ensure `npx next build` passes cleanly with all 4 plans integrated.
  </action>
  <verify>
    <automated>cd C:/Users/ALI/Desktop/ClaudeCodeTest/frontend && npx next build 2>&1 | tail -10</automated>
  </verify>
  <done>Session warning modal renders in authenticated layout, root URL redirects appropriately, login page redirects authenticated users to dashboard, build passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Phase 10 foundation</name>
  <files>frontend/src/app/(app)/layout.tsx</files>
  <action>
Human verifies the complete Phase 10 foundation visually. Claude has already automated all code changes in Task 1.

What was built:
- Next.js 16 app with Better Auth login, 4-role RBAC, Prisma/Neon schema
- Dark sidebar navigation with red active accent, collapsible, responsive
- Breadcrumbs in top header, user avatar dropdown with logout
- Session timeout warning modal (5 min before expiry)
- Password reset via 6-digit email code
- Role-gated placeholder pages with "Keine Berechtigung" for unauthorized access

How to verify:

WITHOUT database (build verification):
1. Run `cd frontend && npx next build` -- should complete without errors
2. Check that all route files exist in src/app/

WITH database (full verification):
1. Set up a Neon Postgres database and update .env.local with real DATABASE_URL and DIRECT_URL
2. Run `cd frontend && npx prisma migrate dev --name init` to create tables
3. Run `npm run dev` -- app starts on http://localhost:3000
4. Visit http://localhost:3000 -- should redirect to /login
5. Login page: FTAG-branded centered card with email/password fields
6. Click "Passwort vergessen?" -- navigates to password reset page with 2-step flow
7. After login: see dashboard with dark charcoal sidebar on left
8. Sidebar: 5 items with icons, active item has red accent
9. Click sidebar collapse toggle -- shrinks to icon-only mode
10. Resize to mobile -- hamburger menu appears, opens as drawer
11. Breadcrumbs in header show current location
12. User menu dropdown has "Abmelden" option
13. Navigate to /admin as non-admin -- see "Keine Berechtigung" message
  </action>
  <verify>Human visual inspection confirms all items above</verify>
  <done>User approves the Phase 10 foundation or describes issues to fix</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx next build` passes cleanly
2. Root URL (/) redirects to /dashboard or /login based on auth state
3. Login page redirects already-authenticated users to /dashboard
4. Session warning modal appears in authenticated pages (when idle timer triggers)
5. All 5 sidebar nav items work and breadcrumbs update on navigation
6. Admin page shows "Keine Berechtigung" for non-admin users
7. Responsive: desktop (expanded sidebar), tablet (icon sidebar), mobile (hamburger drawer)
</verification>

<success_criteria>
- Build passes with zero errors
- Session timeout warning wired into authenticated layout
- Root redirect works for both auth states
- Visual checkpoint confirms FTAG branding, sidebar, breadcrumbs, and responsive behavior
- End-to-end flow: login -> navigate -> logout verified
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation/10-04-SUMMARY.md`
</output>
