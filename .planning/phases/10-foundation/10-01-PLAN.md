---
phase: 10-foundation
plan: 01
type: execute
wave: 1
depends_on: ["10-00"]
files_modified:
  - frontend/package.json
  - frontend/next.config.ts
  - frontend/tsconfig.json
  - frontend/prisma/schema.prisma
  - frontend/prisma.config.ts
  - frontend/src/app/layout.tsx
  - frontend/src/app/page.tsx
  - frontend/src/styles/globals.css
  - frontend/src/lib/prisma.ts
  - frontend/src/lib/auth.ts
  - frontend/src/lib/auth-client.ts
  - frontend/src/lib/permissions.ts
  - frontend/src/lib/utils.ts
  - frontend/src/lib/seed-admin.ts
  - frontend/src/app/api/auth/[...all]/route.ts
  - frontend/.env.local
  - frontend/components.json
autonomous: true
requirements:
  - INFRA-01
  - UI-01
  - AUTH-04

must_haves:
  truths:
    - "Next.js 16 app starts without errors on localhost:3000"
    - "Prisma schema includes User, Session, Account, Verification models with admin plugin fields"
    - "Better Auth server config exports auth instance with RBAC (4 roles) and emailOTP plugins"
    - "Tailwind CSS 4 theme has FTAG Rot/Weiss design tokens (primary=#C8102E, dark charcoal sidebar)"
    - "shadcn/ui is initialized with FTAG-themed components available"
  artifacts:
    - path: "frontend/package.json"
      provides: "Project manifest with all dependencies"
    - path: "frontend/prisma/schema.prisma"
      provides: "Database schema with Better Auth models"
      contains: "model User"
    - path: "frontend/src/lib/auth.ts"
      provides: "Better Auth server configuration"
      exports: ["auth"]
    - path: "frontend/src/lib/permissions.ts"
      provides: "RBAC role and permission definitions"
      exports: ["ac", "viewerRole", "analystRole", "managerRole", "adminRole"]
    - path: "frontend/src/styles/globals.css"
      provides: "Tailwind CSS 4 theme with FTAG tokens"
      contains: "--ftag-red"
  key_links:
    - from: "frontend/src/lib/auth.ts"
      to: "frontend/src/lib/prisma.ts"
      via: "prismaAdapter import"
      pattern: "prismaAdapter\\(prisma"
    - from: "frontend/src/lib/auth.ts"
      to: "frontend/src/lib/permissions.ts"
      via: "access control import"
      pattern: "import.*permissions"
    - from: "frontend/src/app/api/auth/[...all]/route.ts"
      to: "frontend/src/lib/auth.ts"
      via: "auth handler export"
      pattern: "auth\\.handler"
---

<objective>
Scaffold the Next.js 16 project with Better Auth, Prisma 7 + Neon Postgres, and FTAG design system foundation.

Purpose: Create the greenfield frontend/ directory that all subsequent plans build into. This is the v2.0 application shell -- completely separate from the existing frontend-react/ (v1.0).
Output: Working Next.js app with configured auth backend, database schema, and FTAG-branded Tailwind theme.
</objective>

<execution_context>
@C:/Users/ALI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ALI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-foundation/10-CONTEXT.md
@.planning/phases/10-foundation/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 project with dependencies</name>
  <files>
    frontend/package.json,
    frontend/next.config.ts,
    frontend/tsconfig.json,
    frontend/.env.local,
    frontend/components.json
  </files>
  <action>
Create the Next.js 16 project in `frontend/` directory (NOT frontend-react/ which is v1.0):

1. Run `npx create-next-app@latest frontend --typescript --tailwind --eslint --app --src-dir` from the project root. Accept defaults.

2. Install auth and database dependencies:
```
cd frontend && npm install better-auth @prisma/client @prisma/adapter-pg prisma lucide-react sonner tw-animate-css
```

3. Initialize shadcn/ui v4:
```
npx shadcn@latest init
```
When prompted: use default style, use CSS variables, set globals.css path to `src/styles/globals.css`. This creates `components.json` and sets up the `src/components/ui/` directory.

4. Add commonly needed shadcn components:
```
npx shadcn@latest add button input label card dialog alert
```

5. Create `.env.local` with placeholder values:
```
DATABASE_URL="postgresql://user:pass@ep-xxx-pooler.region.neon.tech/dbname?sslmode=require"
DIRECT_URL="postgresql://user:pass@ep-xxx.region.neon.tech/dbname?sslmode=require"
BETTER_AUTH_SECRET="generate-with-npx-auth-secret"
BETTER_AUTH_URL="http://localhost:3000"
DEFAULT_ADMIN_EMAIL="admin@franktueren.ch"
DEFAULT_ADMIN_PASSWORD="ChangeMeOnFirstLogin!"
```

6. Update `next.config.ts` to set `serverExternalPackages: ["better-auth"]` if needed for compatibility.

IMPORTANT: This is a NEW project in `frontend/`. Do NOT touch `frontend-react/`.
  </action>
  <verify>
    <automated>cd C:/Users/ALI/Desktop/ClaudeCodeTest/frontend && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Next.js 16 project exists in frontend/ with all dependencies installed, shadcn/ui initialized, .env.local template created</done>
</task>

<task type="auto">
  <name>Task 2: Configure Prisma 7, Better Auth, and RBAC permissions</name>
  <files>
    frontend/prisma/schema.prisma,
    frontend/prisma.config.ts,
    frontend/src/lib/prisma.ts,
    frontend/src/lib/auth.ts,
    frontend/src/lib/auth-client.ts,
    frontend/src/lib/permissions.ts,
    frontend/src/lib/seed-admin.ts,
    frontend/src/app/api/auth/[...all]/route.ts
  </files>
  <action>
**Prisma 7 Setup:**
1. Create `prisma/schema.prisma` with Better Auth models (User, Session, Account, Verification) exactly as specified in RESEARCH.md Pattern 4 / Prisma Schema section. Include:
   - generator output to `../src/generated/prisma/client`
   - datasource with `url = env("DATABASE_URL")` and `directUrl = env("DIRECT_URL")`
   - User model with role (default "viewer"), banned, banReason, banExpiresAt fields
   - All four Better Auth required models with @@map to lowercase table names

2. Create `src/lib/prisma.ts` -- PrismaClient singleton with PrismaPg adapter, exactly as in RESEARCH.md Pattern 4. Import from `@/generated/prisma/client`.

3. Run `npx prisma generate` to generate the client. Do NOT run migrations yet (needs real Neon DB).

**Better Auth Configuration:**
4. Create `src/lib/permissions.ts` -- Define the access control statement and 4 tiered roles exactly as in RESEARCH.md Pattern 1:
   - Resources: analysis, project, catalog, admin
   - Roles: viewer (read only), analyst (+create analysis), manager (+CRUD projects/catalog), admin (+admin access)
   - Export: ac, viewerRole, analystRole, managerRole, adminRole

5. Create `src/lib/auth.ts` -- Better Auth server config exactly as in RESEARCH.md Pattern 1:
   - prismaAdapter with postgresql provider
   - emailAndPassword enabled with `disableSignUp: true` (invite-only per CONTEXT.md)
   - session expiresIn: 8 hours (28800 seconds), updateAge: 15 minutes (900 seconds)
   - admin plugin with all 4 roles, defaultRole "viewer", adminRoles ["admin"]
   - emailOTP plugin with 6-digit codes, 10-minute expiry, console.log placeholder for sending
   - nextCookies() plugin (must be last)
   - trustedOrigins from BETTER_AUTH_URL env var

6. Create `src/lib/auth-client.ts` -- Better Auth React client as in RESEARCH.md Code Examples section. Export authClient, signIn, signUp, signOut, useSession.

7. Create `src/app/api/auth/[...all]/route.ts` -- Better Auth API route handler:
```typescript
import { auth } from "@/lib/auth";
import { toNextJsHandler } from "better-auth/next-js";
export const { GET, POST } = toNextJsHandler(auth.handler);
```

8. Create `src/lib/seed-admin.ts` -- Admin seeding function as in RESEARCH.md Pattern 5. Uses auth.api.createUser to create admin from DEFAULT_ADMIN_EMAIL/DEFAULT_ADMIN_PASSWORD env vars if no admin exists.

IMPORTANT anti-patterns to avoid (from RESEARCH.md):
- Do NOT use middleware.ts -- Next.js 16 uses proxy.ts (created in Plan 02)
- Do NOT import from '@prisma/client' -- use '@/generated/prisma/client'
  </action>
  <verify>
    <automated>cd C:/Users/ALI/Desktop/ClaudeCodeTest/frontend && npx prisma generate 2>&1 | tail -5</automated>
  </verify>
  <done>Prisma schema generates without errors, Better Auth config exports auth instance with admin + emailOTP plugins, RBAC permissions define 4 tiered roles</done>
</task>

<task type="auto">
  <name>Task 3: Set up FTAG Tailwind theme and root layout</name>
  <files>
    frontend/src/styles/globals.css,
    frontend/src/app/layout.tsx,
    frontend/src/app/page.tsx,
    frontend/src/lib/utils.ts
  </files>
  <action>
**FTAG Design System:**
1. Replace `src/styles/globals.css` with the FTAG Tailwind CSS 4 theme exactly as in RESEARCH.md Pattern 3:
   - :root variables for FTAG Red (#C8102E), sidebar charcoal, semantic tokens
   - @theme inline block mapping all CSS vars to Tailwind utilities
   - @import "tailwindcss" and @import "tw-animate-css"
   - --font-sans: "Inter" as primary

2. Update `src/app/layout.tsx`:
    - Import Inter from `next/font/google`
    - Apply Inter font class to html element
    - Import globals.css from `@/styles/globals.css`
    - Add basic metadata: title "FTAG Angebotserstellung", description

3. Create minimal `src/app/page.tsx` that redirects to /login (temporary, replaced in Plan 04).

4. Create `src/lib/utils.ts` with the `cn()` helper (clsx + twMerge) that shadcn/ui expects.

IMPORTANT anti-patterns to avoid (from RESEARCH.md):
- Do NOT use tailwind.config.js -- CSS-first @theme only
- Do NOT use tailwindcss-animate -- use tw-animate-css
- Do NOT use forwardRef -- React 19 uses function components with data-slot
  </action>
  <verify>
    <automated>cd C:/Users/ALI/Desktop/ClaudeCodeTest/frontend && npx next build 2>&1 | tail -10</automated>
  </verify>
  <done>FTAG theme tokens defined in globals.css with --ftag-red and @theme inline block, root layout uses Inter font, shadcn/ui components work with FTAG primary color, build passes</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx next build` completes without errors
2. `npx prisma generate` succeeds (generates client to src/generated/prisma/client/)
3. `globals.css` contains `--ftag-red` and `@theme inline` block
4. `src/lib/auth.ts` exports `auth` with admin + emailOTP plugins
5. `src/lib/permissions.ts` exports 4 role definitions
6. `.env.local` exists with all required placeholder variables
</verification>

<success_criteria>
- Next.js 16 project in frontend/ builds successfully
- Prisma 7 schema has 4 Better Auth models with admin fields
- Better Auth configured with invite-only email/password, RBAC (4 roles), emailOTP
- Tailwind CSS 4 theme has FTAG Rot/Weiss tokens
- shadcn/ui initialized with button, input, label, card, dialog, alert components
- Auth API route handler registered at /api/auth/[...all]
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation/10-01-SUMMARY.md`
</output>
