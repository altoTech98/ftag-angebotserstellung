---
phase: 08-quality-observability-end-to-end
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/v2/validation/plausibility.py
  - backend/v2/pipeline_logging.py
  - backend/v2/exceptions.py
  - backend/tests/test_plausibility.py
  - backend/tests/test_pipeline_logging.py
  - backend/tests/test_error_handling.py
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-04]

must_haves:
  truths:
    - "Plausibility check detects uncovered positions, duplicates, and suspicious all-match/no-match patterns"
    - "Every pipeline step is logged with tender_id, stage, position_nr, pass_num, and result"
    - "AI service failure raises PipelineError with German user-facing message instead of returning partial results"
  artifacts:
    - path: "backend/v2/validation/plausibility.py"
      provides: "PlausibilityChecker with deterministic validation rules"
      exports: ["check_plausibility", "PlausibilityResult", "PlausibilityIssue", "IssueSeverity"]
    - path: "backend/v2/pipeline_logging.py"
      provides: "Structured step logging helper"
      exports: ["log_step"]
    - path: "backend/v2/exceptions.py"
      provides: "PipelineError with German messages (already exists, extend if needed)"
      contains: "class PipelineError"
    - path: "backend/tests/test_plausibility.py"
      provides: "Unit tests for plausibility checker"
    - path: "backend/tests/test_pipeline_logging.py"
      provides: "Unit tests for structured logging"
    - path: "backend/tests/test_error_handling.py"
      provides: "Unit tests for fail-fast error handling"
  key_links:
    - from: "backend/v2/validation/plausibility.py"
      to: "v2/schemas/matching.py + v2/schemas/adversarial.py"
      via: "imports MatchResult, AdversarialResult types"
      pattern: "from v2\\.schemas"
    - from: "backend/v2/pipeline_logging.py"
      to: "Python logging stdlib"
      via: "logging.getLogger with extra fields"
      pattern: "logger\\.info.*extra="
---

<objective>
Build the quality assurance foundation: plausibility validation, structured step logging, and fail-fast error handling.

Purpose: These three services are consumed by Plan 02 when wiring the full pipeline with SSE streaming. They must exist independently and be tested before integration.
Output: PlausibilityChecker service, log_step helper, updated exception handling, all with unit tests.
</objective>

<execution_context>
@C:/Users/ALI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ALI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-quality-observability-end-to-end/08-RESEARCH.md

<interfaces>
<!-- Existing types the executor needs -->

From backend/v2/schemas/matching.py (MatchResult):
- MatchResult has: positions_nr, hat_match, bester_match (with gesamt_konfidenz)

From backend/v2/schemas/adversarial.py (AdversarialResult):
- AdversarialResult has: positions_nr, validation_status, adjusted_confidence

From backend/v2/schemas/extraction.py (ExtractedDoorPosition):
- ExtractedDoorPosition has: positions_nr field

From backend/v2/exceptions.py:
```python
class V2Error(Exception):
    def __init__(self, message: str, code: str = "V2_ERROR", details: dict = None)

class PipelineError(V2Error):
    def __init__(self, message: str, details: dict = None)
```

From backend/v2/schemas/pipeline.py:
```python
class PipelineStage(str, Enum):
    PARSING, EXTRACTION, MATCHING, VALIDATION, GAP_ANALYSE, OUTPUT

class StageStatus(str, Enum):
    PENDING, RUNNING, COMPLETED, FAILED, SKIPPED
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PlausibilityChecker service and step logging helper</name>
  <files>backend/v2/validation/plausibility.py, backend/v2/pipeline_logging.py, backend/tests/test_plausibility.py, backend/tests/test_pipeline_logging.py</files>
  <behavior>
    - Test: check_plausibility returns PlausibilityResult with passed=False and UNCOVERED_POSITIONS error when positions exist without match results
    - Test: check_plausibility detects duplicate position numbers and returns WARNING-level DUPLICATE_POSITIONS issue
    - Test: check_plausibility flags ALL_MATCHED as WARNING only when >10 positions (not for small tenders <=10)
    - Test: check_plausibility flags NONE_MATCHED as WARNING only when >10 positions
    - Test: check_plausibility flags IDENTICAL_CONFIDENCES when all confidence values are the same and >5 positions
    - Test: check_plausibility returns passed=True with no issues for a healthy result set
    - Test: log_step writes structured log entry with tender_id, stage, position_nr, pass_num, result in extra dict
  </behavior>
  <action>
    1. Create `backend/v2/validation/plausibility.py`:
       - IssueSeverity enum: INFO, WARNING, ERROR
       - PlausibilityIssue Pydantic model: code (str), severity (IssueSeverity), message (str), details (dict)
       - PlausibilityResult Pydantic model: passed (bool), issues (list[PlausibilityIssue]), positions_total (int), positions_matched (int), positions_unmatched (int), duplicate_positions (list[str])
       - `check_plausibility(positions, match_results, adversarial_results, gap_reports) -> PlausibilityResult` function
       - Check 1: UNCOVERED_POSITIONS (ERROR) - positions without match results
       - Check 2: DUPLICATE_POSITIONS (WARNING) - duplicate position numbers
       - Check 3: ALL_MATCHED (WARNING) - 100% match rate with >10 positions
       - Check 4: NONE_MATCHED (WARNING) - 0% match rate with >10 positions
       - Check 5: IDENTICAL_CONFIDENCES (WARNING) - all confidence values identical with >5 positions
       - passed = no ERROR-level issues
       - Use research code example as reference but import actual schema types

    2. Create `backend/v2/pipeline_logging.py`:
       - `log_step(tender_id, stage, position_nr, pass_num, result, details=None)` function
       - Uses `logging.getLogger("v2.pipeline")`
       - Log at INFO level with formatted message: `[{tender_id}] {stage} | Position {position_nr} | Pass {pass_num} | Result: {result}`
       - Pass all params as `extra` dict for structured JSON logging

    3. Create test files with pytest, using mock/fixture objects for MatchResult and ExtractedDoorPosition:
       - `backend/tests/test_plausibility.py` - all behavior cases above
       - `backend/tests/test_pipeline_logging.py` - verify log_step produces correct log records
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_plausibility.py tests/test_pipeline_logging.py -x -v</automated>
  </verify>
  <done>PlausibilityChecker correctly detects all 5 issue types with size-aware thresholds. log_step writes structured entries. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail-fast error handling for AI service failures</name>
  <files>backend/v2/exceptions.py, backend/tests/test_error_handling.py</files>
  <behavior>
    - Test: PipelineError with code="AI_SERVICE_ERROR" carries German message and stage details
    - Test: AIServiceError subclass of PipelineError exists with automatic code and German message template
    - Test: Converting anthropic.APIError to AIServiceError preserves stage context
    - Test: Converting anthropic.APIConnectionError to AIServiceError uses "KI-Service nicht erreichbar" message
  </behavior>
  <action>
    1. Extend `backend/v2/exceptions.py`:
       - Add `AIServiceError(PipelineError)` subclass:
         ```python
         class AIServiceError(PipelineError):
             def __init__(self, message: str, stage: str, api_error: str = ""):
                 super().__init__(
                     message=message,
                     details={"stage": stage, "api_error": api_error},
                 )
                 self.code = "AI_SERVICE_ERROR"
         ```
       - Add helper function `raise_ai_error(exception, stage: str)` that maps common anthropic exceptions to German messages:
         - APIConnectionError -> "KI-Service nicht erreichbar. Bitte Internetverbindung pruefen."
         - RateLimitError -> "KI-Service ueberlastet. Bitte in einigen Minuten erneut versuchen."
         - APIStatusError -> "KI-Service Fehler ({status_code}). Bitte spaeter erneut versuchen."
         - Generic Exception -> "KI-Service nicht verfuegbar: {error}. Bitte spaeter erneut versuchen."

    2. Create `backend/tests/test_error_handling.py`:
       - Test AIServiceError creation with stage context
       - Test raise_ai_error mapping for each exception type (mock anthropic exceptions)
       - Test that German messages are correct
       - Test that details dict includes stage and api_error
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_error_handling.py -x -v</automated>
  </verify>
  <done>AIServiceError subclass and raise_ai_error helper produce German error messages for all common anthropic exception types. Plan 02 can use raise_ai_error(e, "matching") to convert any AI failure to a user-facing error.</done>
</task>

</tasks>

<verification>
All three test files pass:
```bash
cd backend && python -m pytest tests/test_plausibility.py tests/test_pipeline_logging.py tests/test_error_handling.py -x -v
```
</verification>

<success_criteria>
- PlausibilityChecker validates 5 issue types with size-aware thresholds
- log_step produces structured log records with all required fields
- AIServiceError maps anthropic exceptions to German user-facing messages
- All unit tests green
</success_criteria>

<output>
After completion, create `.planning/phases/08-quality-observability-end-to-end/08-01-SUMMARY.md`
</output>
