---
phase: 12-file-handling-project-management
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - frontend/src/app/(app)/projekte/page.tsx
  - frontend/src/app/(app)/projekte/neu/page.tsx
  - frontend/src/app/(app)/projekte/[id]/page.tsx
  - frontend/src/components/projects/project-list.tsx
  - frontend/src/components/projects/project-card.tsx
  - frontend/src/components/projects/project-form.tsx
  - frontend/src/components/projects/archive-dialog.tsx
  - frontend/src/components/upload/file-dropzone.tsx
  - frontend/src/components/upload/file-list.tsx
  - frontend/src/__tests__/upload/file-dropzone.test.ts
  - frontend/src/__tests__/projects/project-detail.test.ts
  - frontend/src/__tests__/projects/project-archive.test.ts
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
  - PROJ-03
  - ANLZ-01

must_haves:
  truths:
    - "User can see a list of their projects (owned + shared) on /projekte"
    - "User can create a new project with name, customer, deadline, description"
    - "User can view a project detail page showing uploaded files and past analyses"
    - "User can drag-and-drop upload PDF/DOCX/XLSX files into a project"
    - "User can archive and delete projects with confirmation dialog"
  artifacts:
    - path: "frontend/src/app/(app)/projekte/page.tsx"
      provides: "Project list page (replaces placeholder)"
      min_lines: 20
    - path: "frontend/src/app/(app)/projekte/neu/page.tsx"
      provides: "Create project page with form"
      min_lines: 15
    - path: "frontend/src/app/(app)/projekte/[id]/page.tsx"
      provides: "Project detail page with files and analyses"
      min_lines: 30
    - path: "frontend/src/components/upload/file-dropzone.tsx"
      provides: "Drag-and-drop upload component using @vercel/blob/client"
      min_lines: 40
    - path: "frontend/src/components/projects/archive-dialog.tsx"
      provides: "Archive/delete confirmation dialog"
      min_lines: 20
  key_links:
    - from: "frontend/src/components/upload/file-dropzone.tsx"
      to: "/api/upload"
      via: "upload() from @vercel/blob/client with handleUploadUrl"
      pattern: "handleUploadUrl.*api/upload"
    - from: "frontend/src/app/(app)/projekte/neu/page.tsx"
      to: "frontend/src/lib/actions/project-actions.ts"
      via: "createProject server action"
      pattern: "createProject"
    - from: "frontend/src/app/(app)/projekte/[id]/page.tsx"
      to: "frontend/src/lib/prisma.ts"
      via: "prisma.project.findUnique with files and analyses includes"
      pattern: "prisma\\.project\\.findUnique"
    - from: "frontend/src/components/projects/archive-dialog.tsx"
      to: "frontend/src/lib/actions/project-actions.ts"
      via: "archiveProject and deleteProject server actions"
      pattern: "(archiveProject|deleteProject)"
---

<objective>
Build the project management UI: project list page, create project form, project detail page with file upload dropzone, and archive/delete functionality.

Purpose: Users need a visual interface to create projects, upload tender documents, view analysis history, and manage project lifecycle.
Output: Three pages (/projekte, /projekte/neu, /projekte/[id]) plus reusable components (project-card, file-dropzone, file-list, archive-dialog).
</objective>

<execution_context>
@C:/Users/ALI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ALI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-file-handling-project-management/12-RESEARCH.md
@.planning/phases/12-file-handling-project-management/12-01-SUMMARY.md

@frontend/src/lib/prisma.ts
@frontend/src/lib/auth.ts
@frontend/src/lib/permissions.ts
@frontend/src/lib/actions/project-actions.ts
@frontend/src/lib/actions/file-actions.ts
@frontend/src/app/api/upload/route.ts

<interfaces>
<!-- From Plan 01 outputs -->

From frontend/src/lib/actions/project-actions.ts:
```typescript
export async function createProject(formData: FormData): Promise<Project>;
export async function archiveProject(projectId: string): Promise<void>;
export async function deleteProject(projectId: string): Promise<void>;
```

From frontend/src/lib/actions/file-actions.ts:
```typescript
export async function saveFileMetadata(input: {
  name: string; blobUrl: string; downloadUrl: string;
  size: number; contentType: string; projectId: string;
}): Promise<File>;
export async function deleteFile(fileId: string): Promise<void>;
```

From frontend/prisma/schema.prisma (after Plan 01):
- Project: id, name, customer?, deadline?, description?, status, ownerId, files[], analyses[], shares[]
- File: id, name, blobUrl, downloadUrl, size, contentType, projectId, uploadedBy
- Analysis: id, projectId, status, result?, startedAt?, endedAt?, startedBy

From shadcn/ui available components:
- card, button, input, label, dialog, dropdown-menu, alert
- All from @/components/ui/*
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project list page + create project form</name>
  <files>
    frontend/src/app/(app)/projekte/page.tsx,
    frontend/src/app/(app)/projekte/neu/page.tsx,
    frontend/src/components/projects/project-list.tsx,
    frontend/src/components/projects/project-card.tsx,
    frontend/src/components/projects/project-form.tsx,
    frontend/src/__tests__/projects/project-detail.test.ts
  </files>
  <behavior>
    - Test: ProjectList renders project cards with name, customer, file count, analysis count
    - Test: ProjectForm submits formData with name (required), customer, deadline, description
    - Test: Empty project list shows "Keine Projekte" message with link to create
  </behavior>
  <action>
1. Create `frontend/src/components/projects/project-card.tsx` (client component):
   - Card layout showing project name (bold), customer, deadline (formatted), status badge
   - Footer: file count icon + number, analysis count icon + number
   - Click navigates to `/projekte/{id}`
   - Dropdown menu (three-dot) with: Archivieren, Loeschen options
   - Use Card, DropdownMenu from shadcn/ui, Folder/FileText/Archive/Trash icons from lucide-react

2. Create `frontend/src/components/projects/project-list.tsx` (server component wrapper):
   - Receives projects array as prop
   - Grid layout: 3 columns on lg, 2 on md, 1 on sm (Tailwind responsive grid)
   - Empty state: centered message "Keine Projekte vorhanden" with Button linking to /projekte/neu
   - Maps projects to ProjectCard components

3. Replace `frontend/src/app/(app)/projekte/page.tsx` (server component):
   - Authenticate via auth.api.getSession
   - Query projects: prisma.project.findMany where ownerId = userId OR shares contains userId, status != archived (unless ?archiv=true query param), include _count for files and analyses, include owner name
   - Order by updatedAt desc
   - Render: heading "Projekte", Button "Neues Projekt" (links to /projekte/neu), toggle for archived, ProjectList component
   - Show archived projects in a separate section if ?archiv=true

4. Create `frontend/src/components/projects/project-form.tsx` (client component):
   - Form with fields: Name (required, Input), Kunde (Input), Frist (Input type="date"), Beschreibung (textarea)
   - Form action calls createProject server action
   - On success, redirect to /projekte via useRouter
   - Show validation error if name is empty
   - Use Button, Input, Label from shadcn/ui
   - Show loading state on submit button (useTransition)

5. Create `frontend/src/app/(app)/projekte/neu/page.tsx` (server component):
   - Heading "Neues Projekt", breadcrumb hint
   - Render ProjectForm component

6. Create test file with behavior tests. Mock prisma and router.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/projects/ --reporter=verbose</automated>
  </verify>
  <done>Project list page shows owned+shared projects in card grid. Create project form validates and submits. Empty state renders correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project detail page with file upload dropzone</name>
  <files>
    frontend/src/app/(app)/projekte/[id]/page.tsx,
    frontend/src/components/upload/file-dropzone.tsx,
    frontend/src/components/upload/file-list.tsx,
    frontend/src/components/projects/archive-dialog.tsx,
    frontend/src/__tests__/upload/file-dropzone.test.ts,
    frontend/src/__tests__/projects/project-archive.test.ts
  </files>
  <behavior>
    - Test: FileDropzone accepts PDF/DOCX/XLSX via drag-and-drop, rejects other types
    - Test: FileDropzone calls upload() with access:'private', multipart:true, handleUploadUrl:'/api/upload'
    - Test: FileDropzone shows progress bar during upload
    - Test: FileList renders file names, sizes, content types with delete button
    - Test: ArchiveDialog shows confirmation before archiving/deleting
    - Test: Project detail page shows files section and analyses section
  </behavior>
  <action>
1. Create `frontend/src/components/upload/file-dropzone.tsx` (client component):
   - Props: projectId (string), onFileUploaded callback
   - Native HTML5 drag-and-drop (onDragOver, onDragLeave, onDrop)
   - Also include a hidden file input + "Dateien auswaehlen" button for click-to-upload
   - Accept only PDF, DOCX, XLSX (validate MIME type client-side, server validates too)
   - On file drop/select: call `upload(file.name, file, { access: 'private', handleUploadUrl: '/api/upload', multipart: true, onUploadProgress })` from @vercel/blob/client
   - After upload resolves: call `saveFileMetadata` server action with blob details, then call onFileUploaded
   - Visual states: default (dashed border, Upload icon, "PDF, DOCX oder XLSX hierher ziehen"), dragging (primary border + bg), uploading (progress bar with percentage)
   - Handle errors with toast via sonner
   - IMPORTANT: Use `access: 'private'` for confidential tender documents (per user decision)
   - IMPORTANT: Set `addRandomSuffix: true` in upload config (defaults to false in recent @vercel/blob)

2. Create `frontend/src/components/upload/file-list.tsx` (client component):
   - Props: files array, onFileDeleted callback
   - List of uploaded files with: FileText icon, filename, formatted size (KB/MB), content type badge
   - Delete button (Trash icon) per file, calls deleteFile server action, then onFileDeleted
   - Empty state: "Noch keine Dateien hochgeladen"

3. Create `frontend/src/components/projects/archive-dialog.tsx` (client component):
   - Props: projectId, projectName, action ("archive" | "delete"), open, onOpenChange
   - Dialog with warning text explaining the action
   - Archive: "Projekt wird archiviert und erscheint nicht mehr in der aktiven Liste"
   - Delete: "Projekt und alle zugehoerigen Dateien werden unwiderruflich geloescht" (destructive red button)
   - Confirm button calls archiveProject or deleteProject, redirects to /projekte on success
   - Cancel button closes dialog

4. Create `frontend/src/app/(app)/projekte/[id]/page.tsx` (server component):
   - Authenticate, fetch project by ID with files, analyses, owner, shares included
   - 404 if not found or user has no access (not owner, not shared)
   - Layout: Project heading with name, customer, deadline, status badge
   - Section "Dateien" with FileDropzone (client) + FileList (client, hydrated with server data)
   - Section "Analysen" with list of analyses: status badge, startedAt, startedBy -- or empty state "Noch keine Analysen durchgefuehrt" (Phase 13 will add "Neue Analyse" button)
   - Action buttons in header: "Archivieren" and "Loeschen" (open ArchiveDialog)
   - Use sonner toast for success/error feedback on file operations

5. Create test files with behavior tests. Mock @vercel/blob/client upload, prisma, server actions.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/upload/ src/__tests__/projects/ --reporter=verbose</automated>
  </verify>
  <done>Project detail page shows files and analyses. Drag-and-drop upload works with progress. Archive/delete dialogs confirm before action. All tests green.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run --reporter=verbose` -- all tests green
- /projekte page renders project list (server-rendered)
- /projekte/neu page renders create form
- /projekte/[id] page shows project detail with upload zone and analysis history
- Archive and delete flows work with confirmation dialogs
</verification>

<success_criteria>
- Project list shows owned + shared projects in responsive card grid
- Create project form validates name, submits via server action, redirects to list
- Project detail page displays files section with drag-and-drop upload
- Upload uses @vercel/blob client upload with private access and progress
- Archive sets status to "archived", delete removes project + blob files
- Analyses section shows history (empty state for now, Phase 13 fills it)
</success_criteria>

<output>
After completion, create `.planning/phases/12-file-handling-project-management/12-02-SUMMARY.md`
</output>
