---
phase: 09-frontend-v2-offer-feedback-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/v2/routers/upload_v2.py
  - frontend-react/src/utils/v2ResultMapper.js
  - frontend-react/src/services/api.js
  - frontend-react/src/pages/AnalysePage.jsx
  - frontend-react/src/hooks/useSSE.js
autonomous: true
requirements:
  - EXEL-01
  - EXEL-02
  - EXEL-03
  - EXEL-04
  - EXEL-05
  - EXEL-06
  - APII-04
  - APII-05

must_haves:
  truths:
    - "Single-file upload uses v2 pipeline (POST /api/v2/upload/single)"
    - "Folder upload uses v2 pipeline (already working)"
    - "After v2 analysis, results display correctly in ResultsPanel with matched/partial/unmatched sections"
    - "Offer generation calls POST /api/offer/generate with analysis_id"
    - "Offer download calls GET /api/offer/{id}/download"
    - "V1 API functions removed from api.js"
  artifacts:
    - path: "backend/v2/routers/upload_v2.py"
      provides: "POST /api/v2/upload/single endpoint"
      contains: "upload_single"
    - path: "frontend-react/src/utils/v2ResultMapper.js"
      provides: "v2 response to display structure transformer"
      exports: ["mapV2ResultToDisplay"]
    - path: "frontend-react/src/services/api.js"
      provides: "v2 offer, download, feedback API functions"
      contains: "generateV2Offer"
  key_links:
    - from: "frontend-react/src/pages/AnalysePage.jsx"
      to: "frontend-react/src/utils/v2ResultMapper.js"
      via: "mapV2ResultToDisplay(result) call after v2 analysis completes"
      pattern: "mapV2ResultToDisplay"
    - from: "frontend-react/src/pages/AnalysePage.jsx"
      to: "frontend-react/src/services/api.js"
      via: "api.generateV2Offer(analysisId) for offer generation"
      pattern: "generateV2Offer"
    - from: "frontend-react/src/pages/AnalysePage.jsx"
      to: "frontend-react/src/services/api.js"
      via: "api.downloadV2Result(resultId) for Excel download"
      pattern: "downloadV2Result"
---

<objective>
Wire the frontend data layer to v2 pipeline: single-file v2 upload endpoint, v2 result transformer, v2 API functions (offer/download/feedback), and rewire both workflows to use v2 endpoints throughout. Remove v1 API functions.

Purpose: After this plan, both single-file and folder uploads flow through the v2 pipeline end-to-end, including offer generation and download of the 4-sheet Excel.
Output: Working data pipeline from upload through analysis to Excel download, all via v2 endpoints.
</objective>

<execution_context>
@C:/Users/ALI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ALI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-frontend-v2-offer-feedback-wiring/09-CONTEXT.md
@.planning/phases/09-frontend-v2-offer-feedback-wiring/09-RESEARCH.md

<interfaces>
<!-- Backend endpoints already implemented -->

From backend/v2/routers/upload_v2.py:
```python
# POST /api/v2/upload - accepts list[UploadFile], returns {tender_id, files, total_files}
# _tenders dict stores parsed files per tender_id
router = APIRouter(prefix="/api/v2", tags=["V2 Upload"])
```

From backend/routers/offer.py:
```python
# POST /api/offer/generate - takes {"analysis_id": str}, returns {"job_id", "status"}
# GET /api/offer/status/{job_id} - returns job status dict
# GET /api/offer/{id}/download - returns Excel bytes (cache key: v2_result_{id}_xlsx)
class GenerateV2ResultRequest(BaseModel):
    analysis_id: str
```

From backend/v2/routers/feedback_v2.py:
```python
# POST /api/v2/feedback
class FeedbackRequest(BaseModel):
    positions_nr: str
    requirement_summary: str
    original_produkt_id: str
    original_konfidenz: float
    corrected_produkt_id: str
    corrected_produkt_name: str
    correction_reason: str
```

From frontend-react/src/hooks/useSSE.js:
```javascript
// pollJob(jobId, onProgress, statusPath) - handles SSE + fallback
// statusPath determines SSE variant: '/v2/' prefix uses pollV2SSE, '/analyze/status/' uses pollSSE, else pollFallback
```

V2 analysis response shape (top-level keys):
```javascript
{
  tender_id, status, positionen[], match_results[], adversarial_results[],
  gap_results[], analysis_id, total_positionen, total_matches, total_gaps, plausibility
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend single-file endpoint + v2ResultMapper + v2 API functions</name>
  <files>backend/v2/routers/upload_v2.py, frontend-react/src/utils/v2ResultMapper.js, frontend-react/src/services/api.js</files>
  <action>
1. In `backend/v2/routers/upload_v2.py`, add a `POST /upload/single` endpoint:
   - Accepts `file: UploadFile = File(...)` (single file, not list)
   - Creates a new tender_id (uuid4), creates tender entry in `_tenders`
   - Parses the single file using `parse_document(content, filename)`
   - Appends result to tender files
   - Returns `{"tender_id": tender_id, "filename": filename, "format": result.format, "total_files": 1}`
   - Register in main.py if not already auto-registered (check how upload_v2 router is mounted)

2. Create `frontend-react/src/utils/v2ResultMapper.js` with `mapV2ResultToDisplay(v2Result)`:
   - Extract `positionen`, `match_results`, `adversarial_results`, `gap_results`, `analysis_id` from v2Result
   - Build lookups by `positions_nr` for match_results, adversarial_results, gap_results
   - For each position, determine confidence from adversarial `adjusted_confidence` (preferred) or match `bester_match.gesamt_konfidenz`
   - Classify into matched (>=0.95), partial (>=0.60), unmatched (<0.60)
   - Map each position to display item shape: `{position, beschreibung, original_position, confidence, category, reason, matched_products, _v2, gap_items, match_criteria}`
   - `matched_products` array: single entry with product name in `Tuerblatt / Verglasungsart / Rollkasten` key (for table display), plus `_produkt_id`
   - `_v2` object: `{adversarial, match, gaps, dimension_scores}` for detail modal and correction modal
   - `gap_items`: mapped from gap_results gaps array `{field: dimension, detail: abweichung_beschreibung, severity: schweregrad}`
   - `match_criteria`: mapped from dimension_scores `{kriterium: dimension, status: 'ok'|'teilweise'|'fehlt', detail: score + begruendung}`
   - Return `{analysis_id, requirements: {positionen}, matching: {matched, partial, unmatched, summary: {total_positions, matched_count, partial_count, unmatched_count, match_rate}}}`
   - Use the exact transformer code from 09-RESEARCH.md Pattern 1 as reference

3. In `frontend-react/src/services/api.js`, add v2 functions and remove v1 functions:
   - ADD `uploadSingleV2(file)`: FormData with `form.append('files', file)`, POST to `/v2/upload/single`
   - ADD `generateV2Offer(analysisId)`: POST to `/offer/generate` with JSON `{analysis_id: analysisId}`
   - ADD `getV2OfferStatus(jobId)`: GET `/offer/status/${jobId}`
   - ADD `downloadV2Result(resultId, filename)`: fetch blob from `/offer/${resultId}/download`, trigger download via anchor click pattern (same pattern as existing `downloadResult` but with `/offer/` URL path and default filename `Machbarkeitsanalyse_${resultId}.xlsx`)
   - ADD `saveV2Feedback(body)`: POST to `/v2/feedback` with JSON body
   - REMOVE v1 functions: `uploadFile`, `startAnalysis`, `generateResult`, `getResultStatus`, `getResultDownloadUrl`, `downloadResult`, `createSSE`, `getJobStatus`
   - Keep: `uploadFolder` (still used?), `saveFeedback` (remove -- replaced by saveV2Feedback), `searchProducts` (keep -- used by CorrectionModal)
   - Also remove `uploadFolder` since folder workflow uses `uploadFolderV2` already
  </action>
  <verify>
    <automated>cd C:/Users/ALI/Desktop/ClaudeCodeTest/backend && python -c "from v2.routers.upload_v2 import router; routes = [r.path for r in router.routes]; assert '/upload/single' in routes, f'Missing /upload/single in {routes}'; print('OK: /upload/single endpoint exists')"</automated>
  </verify>
  <done>
  - POST /api/v2/upload/single endpoint exists and returns tender_id
  - v2ResultMapper.js exports mapV2ResultToDisplay that transforms v2 response to display structure
  - api.js has all v2 functions (uploadSingleV2, generateV2Offer, getV2OfferStatus, downloadV2Result, saveV2Feedback)
  - V1 API functions removed (uploadFile, startAnalysis, generateResult, createSSE, getJobStatus, etc.)
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewire AnalysePage workflows to v2 pipeline</name>
  <files>frontend-react/src/pages/AnalysePage.jsx, frontend-react/src/hooks/useSSE.js</files>
  <action>
1. In `AnalysePage.jsx`, import `mapV2ResultToDisplay` from `../utils/v2ResultMapper`.

2. Rewrite `runSingleWorkflow(file)`:
   - Upload step: call `api.uploadSingleV2(file)` instead of `api.uploadFile(file)`. Extract `tender_id` from response (not `file_id`). Update step status with filename.
   - Analysis step: call `api.startV2Analysis(tender_id)` instead of `api.startAnalysis(file_id)`. Use `pollJob(job_id, progressCallback, '/v2/analyze/status/')` (same SSE logic as folder workflow).
   - Parse progress JSON same as folder workflow (stageMap, position progress, step updates).
   - After analysis completes: `const mapped = mapV2ResultToDisplay(result)` then `setAnalysis(mapped)`.
   - Update step statuses using `mapped.matching.summary` counts (not `result.requirements` or `result.matching` directly).
   - Offer generation step: call `api.generateV2Offer(mapped.analysis_id)`. Poll with `pollJob(offerJobId, ..., '/offer/status/')`. Note: `/offer/status/` is NOT a v2-prefixed path, so pollJob will use pollFallback (HTTP polling). This is correct.
   - Set offer from offer result: `setOffer({...offerResult, result_id: offerResult.result_id || mapped.analysis_id})`.

3. Rewrite `runFolderWorkflow(files)`:
   - Upload step: already uses `api.uploadFolderV2(files)` -- keep as is.
   - Analysis step: already uses `api.startV2Analysis` and v2 SSE -- keep as is.
   - After analysis completes: add `const mapped = mapV2ResultToDisplay(result)` then `setAnalysis(mapped)`.
   - Update step statuses using `mapped.matching.summary` counts.
   - Offer generation step: change from `api.generateResult(result.requirements, result.matching)` to `api.generateV2Offer(mapped.analysis_id)`. Change poll path from `'/result/status/'` to `'/offer/status/'`.
   - Set offer from offer result same as single workflow.

4. In `ResultsPanel`, update the download button:
   - Change `api.downloadResult(offer.result_id)` to `api.downloadV2Result(offer.result_id)`.

5. Update status labels per user decision to use dual labels:
   - In `ResultsPanel.sections`: "Erfuellbar (Bestaetigt)" / "Teilweise (Unsicher)" / "Nicht erfuellbar (Abgelehnt)"
   - In `PositionDetailModal.statusMap`: same dual labels

6. In `useSSE.js`: The pollJob function routes `/offer/status/` to pollFallback (neither v2 prefix nor `/analyze/status/`). This is correct behavior. No changes needed to useSSE.js unless the offer endpoints support SSE streaming -- they don't (they use job_store polling), so HTTP fallback is appropriate.
  </action>
  <verify>
    <automated>cd C:/Users/ALI/Desktop/ClaudeCodeTest/frontend-react && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>
  - runSingleWorkflow uses v2 upload/single, v2 analyze, v2 offer/generate, v2 download
  - runFolderWorkflow uses v2 upload, v2 analyze, v2 offer/generate, v2 download
  - Both workflows transform results through mapV2ResultToDisplay before setting state
  - Download button calls downloadV2Result with correct /offer/ URL path
  - Status labels show dual format per user decision
  - Frontend builds without errors
  </done>
</task>

</tasks>

<verification>
1. `cd backend && python -c "from v2.routers.upload_v2 import router"` -- no import errors
2. `cd frontend-react && npx vite build` -- builds without errors
3. Check api.js has no v1 functions: grep for `uploadFile`, `startAnalysis`, `generateResult`, `createSSE` should find nothing
4. Check AnalysePage.jsx uses only v2 functions: grep for `mapV2ResultToDisplay`, `generateV2Offer`, `downloadV2Result`
</verification>

<success_criteria>
- Both single and folder workflows use v2 pipeline end-to-end
- V2 result transformer correctly maps positionen/match_results/adversarial_results/gap_results to matched/partial/unmatched display arrays
- Offer generation uses analysis_id (not raw data)
- Download uses /offer/{id}/download (not /result/{id}/download)
- V1 API functions removed
- Frontend builds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/09-frontend-v2-offer-feedback-wiring/09-01-SUMMARY.md`
</output>
