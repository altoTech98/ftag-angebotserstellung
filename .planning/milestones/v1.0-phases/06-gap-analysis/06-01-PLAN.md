---
phase: 06-gap-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/v2/schemas/gaps.py
  - backend/v2/gaps/__init__.py
  - backend/v2/gaps/gap_prompts.py
  - backend/v2/gaps/gap_analyzer.py
  - backend/tests/test_v2_gaps.py
autonomous: true
requirements:
  - GAPA-01
  - GAPA-02
  - GAPA-03
  - GAPA-04
  - GAPA-05

must_haves:
  truths:
    - "Every bestaetigt position with dimension scores <1.0 gets per-dimension gap items"
    - "Every unsicher position gets a full gap report against best-effort match"
    - "Every abgelehnt position gets text summary only (no per-dimension breakdown)"
    - "Safety dimensions (Brandschutz, Schallschutz) are never rated MINOR"
    - "Each gap includes anforderung_wert vs katalog_wert side-by-side"
    - "Each gap has dual suggestions: Kundenvorschlag and Technischer Hinweis"
    - "Up to 3 alternative products per position ranked by gap coverage"
    - "Abgelehnt alternatives filtered to >30% gap coverage only"
  artifacts:
    - path: "backend/v2/schemas/gaps.py"
      provides: "Expanded gap schemas with 6 dimensions, dual suggestions, cross-references"
      contains: "GapDimension.BRANDSCHUTZ"
    - path: "backend/v2/gaps/gap_analyzer.py"
      provides: "Gap analysis engine with Opus calls and TF-IDF alternative search"
      exports: ["analyze_gaps", "analyze_single_position_gaps"]
    - path: "backend/v2/gaps/gap_prompts.py"
      provides: "German system/user prompt templates for gap Opus calls"
      exports: ["GAP_SYSTEM_PROMPT", "GAP_USER_TEMPLATE"]
    - path: "backend/tests/test_v2_gaps.py"
      provides: "Tests for gap schemas, severity escalation, and analyzer"
      min_lines: 80
  key_links:
    - from: "backend/v2/gaps/gap_analyzer.py"
      to: "backend/v2/schemas/gaps.py"
      via: "imports GapReport, GapItem, GapDimension, GapSeverity, AlternativeProduct"
      pattern: "from v2\\.schemas\\.gaps import"
    - from: "backend/v2/gaps/gap_analyzer.py"
      to: "backend/v2/schemas/adversarial.py"
      via: "imports AdversarialResult, ValidationStatus for three-track processing"
      pattern: "from v2\\.schemas\\.adversarial import"
    - from: "backend/v2/gaps/gap_analyzer.py"
      to: "backend/v2/matching/tfidf_index.py"
      via: "uses CatalogTfidfIndex.search() for gap-weighted alternative search"
      pattern: "tfidf_index\\.search"
---

<objective>
Build the gap analysis engine: expanded schemas, German Opus prompts, gap analyzer with three-track processing (bestaetigt/unsicher/abgelehnt), safety auto-escalation, gap-weighted TF-IDF alternative search, and cross-references between gaps and alternatives.

Purpose: Core gap analysis capability — the transformation from Phase 4 MatchResult + Phase 5 AdversarialResult into GapReport with categorized gaps, severity ratings, dual suggestions, and alternative products.

Output: gap_analyzer.py, gap_prompts.py, expanded gaps.py schemas, and test suite.
</objective>

<execution_context>
@C:/Users/ALI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ALI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-gap-analysis/06-CONTEXT.md
@.planning/phases/06-gap-analysis/06-RESEARCH.md

@backend/v2/schemas/gaps.py
@backend/v2/schemas/adversarial.py
@backend/v2/schemas/matching.py
@backend/v2/matching/adversarial.py
@backend/v2/matching/tfidf_index.py
@backend/v2/matching/domain_knowledge.py
@backend/v2/gaps/__init__.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From backend/v2/schemas/matching.py:
```python
class MatchDimension(str, Enum):
    MASSE = "Masse"
    BRANDSCHUTZ = "Brandschutz"
    SCHALLSCHUTZ = "Schallschutz"
    MATERIAL = "Material"
    ZERTIFIZIERUNG = "Zertifizierung"
    LEISTUNG = "Leistung"

class MatchResult(BaseModel):
    positions_nr: str
    bester_match: Optional[MatchCandidate]
    alternative_matches: list[MatchCandidate]
    hat_match: bool
    match_methode: str
```

From backend/v2/schemas/adversarial.py:
```python
class ValidationStatus(str, Enum):
    BESTAETIGT = "bestaetigt"
    UNSICHER = "unsicher"
    ABGELEHNT = "abgelehnt"

class DimensionCoT(BaseModel):
    dimension: str
    score: float
    reasoning: str
    confidence_level: Literal["hoch", "niedrig"]

class AdversarialResult(BaseModel):
    positions_nr: str
    validation_status: ValidationStatus
    adjusted_confidence: float
    bester_match: Optional[AdversarialCandidate]
    per_dimension_cot: list[DimensionCoT]
    ...
```

From backend/v2/matching/tfidf_index.py:
```python
class CatalogTfidfIndex:
    catalog_rows: list[dict]
    def search(self, position_or_match, top_k=50) -> list[tuple[int, float]]:
    def extract_candidate_fields(self, row_idx: int) -> dict:
    def _build_query_from_position(self, pos) -> str:
```

From backend/v2/matching/domain_knowledge.py:
```python
FIRE_CLASS_RANK: dict  # EI30->1, EI60->2, EI90->3, EI120->4
RESISTANCE_RANK: dict  # RC1->1, RC2->2, etc.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand gap schemas and create test suite</name>
  <files>backend/v2/schemas/gaps.py, backend/tests/test_v2_gaps.py</files>
  <behavior>
    - GapDimension enum has 6 values: MASSE, BRANDSCHUTZ, SCHALLSCHUTZ, MATERIAL, ZERTIFIZIERUNG, LEISTUNG (1:1 with MatchDimension)
    - GapItem has fields: dimension, schweregrad, anforderung_wert, katalog_wert, abweichung_beschreibung, kundenvorschlag, technischer_hinweis, gap_geschlossen_durch (list[str])
    - AlternativeProduct has fields: produkt_id, produkt_name, teilweise_deckung, verbleibende_gaps, geschlossene_gaps
    - GapReport has fields: positions_nr, gaps, alternativen, zusammenfassung, validation_status (str for bestaetigt/unsicher/abgelehnt)
    - Safety auto-escalation: GapItem with dimension=BRANDSCHUTZ and schweregrad=MINOR is upgraded to MAJOR
    - Safety auto-escalation: GapItem with dimension=SCHALLSCHUTZ and schweregrad=MINOR is upgraded to MAJOR
    - GapDimension values match MatchDimension values exactly (both use "Masse", "Brandschutz", etc.)
    - Test helper apply_safety_escalation correctly upgrades MINOR to MAJOR for safety dimensions
  </behavior>
  <action>
    1. Expand GapDimension enum in gaps.py: replace NORM with BRANDSCHUTZ and SCHALLSCHUTZ (6 total, matching MatchDimension values exactly).

    2. Expand GapItem: remove aenderungsvorschlag field. Add: kundenvorschlag (Optional[str], sales suggestion), technischer_hinweis (Optional[str], technical suggestion), gap_geschlossen_durch (list[str], default_factory=list, produkt_ids of alternatives that close this gap).

    3. Expand AlternativeProduct: add geschlossene_gaps (list[str], default_factory=list, gap dimensions closed by this alternative).

    4. Expand GapReport: add validation_status (str, the bestaetigt/unsicher/abgelehnt status for context).

    5. Add an internal Pydantic model GapAnalysisResponse for Opus structured output (NOT the final GapReport): gaps (list of gap items with dimension, schweregrad, anforderung_wert, katalog_wert, abweichung_beschreibung, kundenvorschlag, technischer_hinweis), zusammenfassung (str). Use GapDimension and GapSeverity enums in this model so Opus returns valid enum values.

    6. Add a standalone function apply_safety_escalation(gaps: list[GapItem]) -> list[GapItem] in gaps.py that upgrades MINOR to MAJOR for BRANDSCHUTZ and SCHALLSCHUTZ dimensions (deterministic post-processing, mirrors Phase 4 safety cap pattern).

    7. Create tests/test_v2_gaps.py with:
       - TestGapDimensions: verify 6 enum values, verify 1:1 mapping with MatchDimension values
       - TestSeverityEscalation: verify apply_safety_escalation upgrades MINOR->MAJOR for Brandschutz/Schallschutz, leaves MINOR alone for Material/Masse, leaves MAJOR/KRITISCH unchanged
       - TestGapSchemas: verify GapItem, AlternativeProduct, GapReport can be constructed with all new fields
       - TestGapAnalysisResponse: verify the Opus output model accepts valid data and rejects invalid dimensions

    Run tests RED first (confirm they fail before schemas exist with new fields), then make them GREEN.
  </action>
  <verify>
    <automated>cd C:/Users/ALI/Desktop/ClaudeCodeTest/backend && python -m pytest tests/test_v2_gaps.py -x -v</automated>
  </verify>
  <done>GapDimension has 6 values matching MatchDimension, GapItem has dual suggestions and cross-reference, AlternativeProduct has geschlossene_gaps, apply_safety_escalation correctly handles safety dimensions, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Build gap analyzer engine and German prompts</name>
  <files>backend/v2/gaps/gap_prompts.py, backend/v2/gaps/gap_analyzer.py, backend/v2/gaps/__init__.py, backend/tests/test_v2_gaps.py</files>
  <action>
    1. Create gap_prompts.py with German system and user prompt templates:
       - GAP_SYSTEM_PROMPT: You are a door specification expert analyzing gaps between tender requirements and catalog products. Instruct Claude to identify every dimension where the product deviates from the requirement. Output dual suggestions (Kundenvorschlag for sales team, Technischer Hinweis for engineering). All text in German. Emphasize side-by-side values (anforderung_wert vs katalog_wert) for every gap.
       - GAP_USER_TEMPLATE: Template with placeholders for {positions_nr}, {anforderung_felder} (extracted position fields), {produkt_felder} (matched product fields), {dimension_cot} (Phase 5 per-dimension CoT as context — do NOT re-derive scores), {validation_status}. For bestaetigt: instruct to only analyze dimensions with score < 1.0. For unsicher: full analysis all dimensions.
       - GAP_ABGELEHNT_SYSTEM_PROMPT: Simpler prompt for abgelehnt positions — text summary only, no per-dimension breakdown. Focus on why no match exists and what kind of product would be needed.
       - GAP_ABGELEHNT_USER_TEMPLATE: Template with {positions_nr}, {anforderung_felder} only (no product fields since no match).

    2. Create gap_analyzer.py mirroring Phase 5's adversarial.py structure:
       - Constants: GAP_MAX_CONCURRENT = 3, ABGELEHNT_MIN_COVERAGE = 0.3, MAX_ALTERNATIVES = 3
       - SAFETY_DIMENSIONS = {GapDimension.BRANDSCHUTZ, GapDimension.SCHALLSCHUTZ}
       - DIMENSION_TO_TFIDF_FIELDS mapping (Brandschutz -> ["brandschutzklasse"], Schallschutz -> ["schallschutz", "tuerrohling"], Masse -> ["lichtmass"], Material -> ["material"], Zertifizierung -> ["widerstandsklasse"], Leistung -> ["kategorie", "produktegruppen"])
       - GAP_BOOST_MULTIPLIER = 2.0

       Helper functions:
       - _format_position_for_prompt(position: ExtractedDoorPosition) -> str: extract key technical fields as compact text for prompt injection. Skip None fields.
       - _format_product_for_prompt(product_fields: dict) -> str: format matched product fields for prompt.
       - _format_dimension_cot(cot_list: list[DimensionCoT]) -> str: format Phase 5 CoT as context string.
       - build_gap_weighted_query(position: ExtractedDoorPosition, gaps: list[GapItem]) -> str: build TF-IDF query with boosted weights for gap dimensions. For each gap dimension, repeat the corresponding TF-IDF fields GAP_BOOST_MULTIPLIER times more than normal. Use the same field extraction as CatalogTfidfIndex._build_query_from_position but with dynamic boosting.
       - search_alternatives_for_gaps(position, gaps, tfidf_index, matched_product_id, top_k=20) -> list[AlternativeProduct]: Run gap-weighted TF-IDF search, filter out matched_product_id, extract top MAX_ALTERNATIVES candidates. For each candidate, compute which gaps it closes and which remain by comparing candidate fields against anforderung_wert. Set teilweise_deckung = len(geschlossene_gaps) / total_gaps. For abgelehnt: filter to >30% coverage only.
       - _cross_reference_gaps_and_alternatives(gaps, alternatives) -> None: Mutate gap items to set gap_geschlossen_durch from alternatives' geschlossene_gaps. Ensure referential integrity (only produkt_ids in alternativen list).

       Core async functions:
       - analyze_single_position_gaps(client, match_result, adversarial_result, tfidf_index, semaphore) -> GapReport:
         Three-track processing based on adversarial_result.validation_status:
         a) BESTAETIGT: Filter per_dimension_cot to dimensions with score < 1.0. If none, return empty GapReport. Otherwise, Opus call with GAP_SYSTEM_PROMPT + GAP_USER_TEMPLATE, instruct to only analyze non-perfect dimensions. Use asyncio.to_thread wrapping client.messages.parse with GapAnalysisResponse output_format. Apply apply_safety_escalation. Search alternatives. Cross-reference.
         b) UNSICHER: Full Opus call for all dimensions. Same pattern as bestaetigt but no dimension filtering. Apply safety escalation. Search alternatives. Cross-reference.
         c) ABGELEHNT: Opus call with GAP_ABGELEHNT_SYSTEM_PROMPT + GAP_ABGELEHNT_USER_TEMPLATE (text summary only, no GapAnalysisResponse parsing — use plain text response). Search alternatives with >30% filter. Return GapReport with empty gaps list and text zusammenfassung.
         For positions with fewer than 2 technical fields populated: return summary-only GapReport noting "Unzureichende Spezifikationsdaten fuer detaillierte Gap-Analyse".
         Extract product fields using tfidf_index.extract_candidate_fields() for the matched product.

       - analyze_gaps(client, match_results, adversarial_results, tfidf_index) -> list[GapReport]:
         Batch function. Create Semaphore(GAP_MAX_CONCURRENT). Match adversarial_results to match_results by positions_nr. Use asyncio.gather for concurrency. Wrap each call in try/except, return empty GapReport on failure (log error, don't crash pipeline).

    3. Update gaps/__init__.py to export analyze_gaps and analyze_single_position_gaps.

    4. Add integration-style tests to test_v2_gaps.py:
       - TestGapAnalyzer: test analyze_single_position_gaps with mocked Anthropic client (mock client.messages.parse to return a GapAnalysisResponse). Verify:
         * bestaetigt position with one non-perfect dimension produces 1 gap item
         * unsicher position produces gap items for all dimensions
         * abgelehnt position produces empty gaps list with text zusammenfassung
         * safety escalation applied (mock returns Brandschutz as MINOR, verify upgraded to MAJOR)
       - TestAlternativeSearch: test search_alternatives_for_gaps with mocked tfidf_index. Verify:
         * matched product filtered out
         * max 3 alternatives returned
         * abgelehnt filtered to >30% coverage
         * cross-references set correctly (gap_geschlossen_durch contains valid produkt_ids)
  </action>
  <verify>
    <automated>cd C:/Users/ALI/Desktop/ClaudeCodeTest/backend && python -m pytest tests/test_v2_gaps.py -x -v</automated>
  </verify>
  <done>gap_analyzer.py implements three-track processing (bestaetigt/unsicher/abgelehnt), gap_prompts.py has German templates, alternative search with gap-weighted TF-IDF works, cross-references are bidirectional, all tests pass including mocked Opus calls</done>
</task>

</tasks>

<verification>
- All 6 GapDimension values match MatchDimension values
- apply_safety_escalation never allows MINOR for Brandschutz/Schallschutz
- GapItem has dual suggestions (kundenvorschlag, technischer_hinweis) and gap_geschlossen_durch cross-reference
- AlternativeProduct has geschlossene_gaps for bidirectional cross-reference
- Three-track processing: bestaetigt (non-perfect dims only), unsicher (full), abgelehnt (text summary only)
- Gap-weighted TF-IDF search boosts fields for gap dimensions
- Matched product excluded from alternatives
- Abgelehnt alternatives filtered to >30% coverage
- All prompts in German

```bash
cd C:/Users/ALI/Desktop/ClaudeCodeTest/backend && python -m pytest tests/test_v2_gaps.py -x -v
```
</verification>

<success_criteria>
- backend/v2/schemas/gaps.py has expanded schemas with 6 dimensions, dual suggestions, cross-references
- backend/v2/gaps/gap_analyzer.py exports analyze_gaps and analyze_single_position_gaps
- backend/v2/gaps/gap_prompts.py has German system/user templates for gap and abgelehnt
- backend/tests/test_v2_gaps.py passes with schema, escalation, analyzer, and alternative search tests
- No new dependencies needed (all libraries already in project)
</success_criteria>

<output>
After completion, create `.planning/phases/06-gap-analysis/06-01-SUMMARY.md`
</output>
