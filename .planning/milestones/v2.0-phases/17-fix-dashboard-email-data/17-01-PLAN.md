---
phase: 17-fix-dashboard-email-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/actions/dashboard-actions.ts
  - frontend/src/lib/actions/analysis-actions.ts
  - frontend/src/__tests__/dashboard/statistics.test.tsx
  - frontend/src/__tests__/email/analysis-complete.test.tsx
autonomous: true
requirements: [DASH-03, INFRA-05]
gap_closure: true

must_haves:
  truths:
    - "getMatchGapStatistics returns correct match count (matched + partial), gap count (unmatched), and avg confidence (0-1 scale)"
    - "sendAnalysisCompleteEmail includes partial entries in match count and calculates avg confidence as percentage for email display"
  artifacts:
    - path: "frontend/src/lib/actions/dashboard-actions.ts"
      provides: "Fixed getMatchGapStatistics reading matched/partial/unmatched keys"
      contains: "result.matched"
    - path: "frontend/src/lib/actions/analysis-actions.ts"
      provides: "Fixed sendAnalysisCompleteEmail including partial entries"
      contains: "result.partial"
    - path: "frontend/src/__tests__/dashboard/statistics.test.tsx"
      provides: "Unit tests for getMatchGapStatistics"
    - path: "frontend/src/__tests__/email/analysis-complete.test.tsx"
      provides: "Unit tests for sendAnalysisCompleteEmail stats extraction"
  key_links:
    - from: "frontend/src/lib/actions/dashboard-actions.ts"
      to: "Prisma Analysis.result JSON"
      via: "result.matched, result.partial, result.unmatched keys"
      pattern: "result\\.matched|result\\.partial|result\\.unmatched"
    - from: "frontend/src/lib/actions/analysis-actions.ts"
      to: "Prisma Analysis.result JSON"
      via: "result.matched, result.partial, result.unmatched keys"
      pattern: "result\\.partial"
---

<objective>
Fix two data access bugs where Next.js server actions read incorrect JSON keys from the analysis result stored in Prisma.

Purpose: Dashboard statistics widget shows zero/incorrect data because `getMatchGapStatistics` reads non-existent keys (`matched_items`, `gap_items`). Email notification omits partial matches from stats.
Output: Both consumers correctly read `matched`, `partial`, `unmatched` keys from the Python result shape. Tests verify the fix.
</objective>

<execution_context>
@C:/Users/ALI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ALI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-fix-dashboard-email-data/17-RESEARCH.md

Source files to modify:
@frontend/src/lib/actions/dashboard-actions.ts
@frontend/src/lib/actions/analysis-actions.ts

<interfaces>
<!-- Python result shape stored in Prisma Analysis.result JSON column -->
```typescript
// From frontend/src/components/analysis/types.ts
interface AnalysisResult {
  matched: MatchEntry[];    // status="matched", confidence 0-1
  partial: MatchEntry[];    // status="partial", confidence 0-1, has gap_items
  unmatched: MatchEntry[];  // status="unmatched", confidence=0
  summary: {
    total_positions: number;
    matched_count: number;
    partial_count: number;
    unmatched_count: number;
    match_rate: number;
  };
}

interface MatchEntry {
  confidence: number; // 0-1 scale
  // ... other fields
}
```

<!-- Dashboard consumer expects 0-1 confidence (widget does *100) -->
<!-- Email consumer expects percentage confidence (template displays directly with %) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix getMatchGapStatistics to read correct result keys</name>
  <files>frontend/src/lib/actions/dashboard-actions.ts, frontend/src/__tests__/dashboard/statistics.test.tsx</files>
  <behavior>
    - Test 1: Given result with matched=[{confidence:0.9}], partial=[{confidence:0.7}], unmatched=[{}] -> returns totalMatches=2, totalGaps=1, avgConfidence=0.8
    - Test 2: Given result with empty arrays (matched=[], partial=[], unmatched=[]) -> returns totalMatches=0, totalGaps=0, avgConfidence=0
    - Test 3: Given result with only partial entries -> partial entries count as matches with their confidence averaged
    - Test 4: Given null/missing result field -> gracefully returns zeros
  </behavior>
  <action>
1. Implement tests in `frontend/src/__tests__/dashboard/statistics.test.tsx`:
   - Mock `@/lib/prisma` to return controlled analysis records with known result JSON
   - Mock `@/lib/auth` to return a valid session
   - Mock `next/headers` to return empty headers
   - Test the 4 behaviors listed above by varying the mock Prisma return values
   - Tests should verify the function reads `result.matched`, `result.partial`, `result.unmatched` (NOT `matched_items`/`gap_items`)

2. Fix `getMatchGapStatistics` in `frontend/src/lib/actions/dashboard-actions.ts` (lines 87-119):
   - Replace `result.matched_items` with `result.matched` (line 93)
   - Replace `result.gap_items` with `result.unmatched` (line 110)
   - Add `partial` array handling: read `result.partial`, add its length to `totalMatches`, include its confidence values in the average
   - Keep avgConfidence on 0-1 scale (dashboard widget already multiplies by 100)
   - Keep the existing null/type guards and Prisma JSON casting pattern
  </action>
  <verify>
    <automated>cd C:/Users/ALI/Desktop/ClaudeCodeTest/frontend && npx vitest run src/__tests__/dashboard/statistics.test.tsx -x</automated>
  </verify>
  <done>getMatchGapStatistics reads matched/partial/unmatched keys. Tests pass confirming correct counts and confidence calculation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix sendAnalysisCompleteEmail to include partial entries</name>
  <files>frontend/src/lib/actions/analysis-actions.ts, frontend/src/__tests__/email/analysis-complete.test.tsx</files>
  <behavior>
    - Test 1: Given result with matched=[{confidence:0.9}], partial=[{confidence:0.6}], unmatched=[{}] -> email receives matchCount=2, gapCount=1, avgConfidence=75
    - Test 2: Given result with only partial entries (no matched) -> matchCount equals partial count, confidence averaged from partial only
    - Test 3: Given result with no matches and no partial -> matchCount=0, avgConfidence=0
    - Test 4: Given null result -> matchCount=0, gapCount=0, avgConfidence=0
  </behavior>
  <action>
1. Create test file `frontend/src/__tests__/email/analysis-complete.test.tsx`:
   - Mock `@/lib/prisma` to return a controlled analysis record with project and user
   - Mock `@/lib/auth` (not used directly by this function but imported by module)
   - Mock `@/lib/email` (resend.emails.send) to capture the props passed to the email template
   - Mock `next/headers` and `next/cache`
   - Mock `@/emails/analysis-complete` to capture its props
   - Test the 4 behaviors by varying the `analysis.result` mock and asserting the props passed to AnalysisCompleteEmail
   - Verify avgConfidence is a percentage integer (e.g., 75 not 0.75) since the email template displays `{avgConfidence}%`

2. Fix `sendAnalysisCompleteEmail` in `frontend/src/lib/actions/analysis-actions.ts` (lines 160-176):
   - Add: `const partialItems = Array.isArray(result?.partial) ? result.partial : [];` after line 163
   - Change: `const matchCount = matchItems.length + partialItems.length;` (was `matchItems.length` only)
   - Change confidence calculation to average over `[...matchItems, ...partialItems]` instead of just `matchItems`
   - Guard: if `matchCount > 0` (not `matchItems.length > 0`) for confidence calc
   - Keep avgConfidence as percentage integer (Math.round of 0-1 * 100) -- email template displays directly with %
  </action>
  <verify>
    <automated>cd C:/Users/ALI/Desktop/ClaudeCodeTest/frontend && npx vitest run src/__tests__/email/analysis-complete.test.tsx -x</automated>
  </verify>
  <done>sendAnalysisCompleteEmail includes partial entries in matchCount and confidence. Tests pass confirming correct stats passed to email template.</done>
</task>

</tasks>

<verification>
Run both test suites together:
```bash
cd frontend && npx vitest run src/__tests__/dashboard/statistics.test.tsx src/__tests__/email/analysis-complete.test.tsx -x
```

Grep to confirm no remaining references to wrong keys:
```bash
grep -n "matched_items\|gap_items" frontend/src/lib/actions/dashboard-actions.ts frontend/src/lib/actions/analysis-actions.ts
```
Should return zero results.

Confirm partial is handled in both files:
```bash
grep -n "partial" frontend/src/lib/actions/dashboard-actions.ts frontend/src/lib/actions/analysis-actions.ts
```
Should show partial array handling in both files.
</verification>

<success_criteria>
- getMatchGapStatistics reads `result.matched`, `result.partial`, `result.unmatched` (not `matched_items`/`gap_items`)
- totalMatches = matched.length + partial.length
- totalGaps = unmatched.length
- avgConfidence returned on 0-1 scale from dashboard action
- sendAnalysisCompleteEmail includes partial in matchCount and confidence calculation
- avgConfidence in email is percentage integer (0-100)
- All tests pass
- No references to `matched_items` or `gap_items` remain in either file
</success_criteria>

<output>
After completion, create `.planning/phases/17-fix-dashboard-email-data/17-01-SUMMARY.md`
</output>
