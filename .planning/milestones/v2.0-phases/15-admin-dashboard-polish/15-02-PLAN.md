---
phase: 15-admin-dashboard-polish
plan: "02"
type: execute
wave: 2
depends_on: ["15-00"]
files_modified:
  - frontend/src/app/(app)/dashboard/page.tsx
  - frontend/src/app/(app)/dashboard/client.tsx
  - frontend/src/app/(app)/dashboard/stat-cards.tsx
  - frontend/src/app/(app)/dashboard/activity-feed.tsx
  - frontend/src/app/(app)/dashboard/statistics-widget.tsx
  - frontend/src/lib/actions/dashboard-actions.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04

must_haves:
  truths:
    - "User sees status cards showing running, completed, and failed analysis counts"
    - "User sees recent activity feed showing who did what and when"
    - "User sees statistics widget with total matches, gaps, and average confidence"
    - "User sees a prominent 'Neue Analyse starten' button in the page header"
  artifacts:
    - path: "frontend/src/app/(app)/dashboard/page.tsx"
      provides: "Server component with data fetching"
      min_lines: 20
    - path: "frontend/src/app/(app)/dashboard/client.tsx"
      provides: "Dashboard layout client shell"
      min_lines: 30
    - path: "frontend/src/app/(app)/dashboard/stat-cards.tsx"
      provides: "Status card components"
      min_lines: 40
    - path: "frontend/src/app/(app)/dashboard/activity-feed.tsx"
      provides: "Activity feed list"
      min_lines: 40
    - path: "frontend/src/app/(app)/dashboard/statistics-widget.tsx"
      provides: "Match/gap statistics with bar chart"
      min_lines: 40
    - path: "frontend/src/lib/actions/dashboard-actions.ts"
      provides: "Dashboard data fetching server actions"
      exports: ["getDashboardStats", "getMatchGapStatistics"]
  key_links:
    - from: "frontend/src/app/(app)/dashboard/page.tsx"
      to: "frontend/src/lib/actions/dashboard-actions.ts"
      via: "server action calls for initial data"
      pattern: "getDashboardStats|getMatchGapStatistics"
    - from: "frontend/src/lib/actions/dashboard-actions.ts"
      to: "frontend/src/generated/prisma/client"
      via: "Prisma queries on Analysis and AuditLog"
      pattern: "prisma\\.(analysis|auditLog)"
    - from: "frontend/src/app/(app)/dashboard/client.tsx"
      to: "frontend/src/app/(app)/dashboard/stat-cards.tsx"
      via: "component composition"
      pattern: "StatCards"

---

<objective>
Dashboard page: status cards for analysis counts, recent activity feed, match/gap statistics widget, and quick-action button to start new analysis.

Purpose: Gives all users an at-a-glance overview of system activity and analysis health, with a fast path to start new work.
Output: Complete dashboard replacing the placeholder, matching Linear's clean dashboard feel.
</objective>

<execution_context>
@C:/Users/ALI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ALI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-admin-dashboard-polish/15-00-SUMMARY.md
@frontend/src/lib/actions/project-actions.ts

<interfaces>
From frontend/prisma/schema.prisma:
```prisma
model Analysis {
  id        String    @id @default(cuid())
  projectId String
  status    String    @default("pending")  // "pending", "running", "completed", "failed"
  result    Json?     // Contains matches/gaps data
  startedAt DateTime?
  endedAt   DateTime?
  startedBy String
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
}

model AuditLog {
  id         String   @id @default(cuid())
  userId     String
  action     String
  details    String
  targetId   String?
  targetType String?
  createdAt  DateTime @default(now())
}
```

From frontend/src/lib/actions/audit-actions.ts (created in 15-00):
```typescript
export async function getActivityFeed(limit?: number): Promise<AuditEntry[]>;
```

From frontend/src/components/ui/card.tsx:
```typescript
export { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard server actions + data layer</name>
  <files>frontend/src/lib/actions/dashboard-actions.ts</files>
  <action>
Create `frontend/src/lib/actions/dashboard-actions.ts` with 'use server' directive.

1. `getDashboardStats()`:
   - Auth check (any authenticated user can view dashboard)
   - Query `prisma.analysis.groupBy({ by: ['status'], _count: { id: true } })`
   - Transform into: `{ running: number, completed: number, failed: number, total: number }`
   - Query `prisma.auditLog.findMany({ take: 20, orderBy: { createdAt: 'desc' }, include: { user: { select: { name: true, email: true } } } })`
   - Return `{ stats: { running, completed, failed, total }, recentActivity: AuditEntry[] }`

2. `getMatchGapStatistics()`:
   - Auth check
   - Fetch completed analyses: `prisma.analysis.findMany({ where: { status: 'completed', result: { not: null } }, select: { result: true }, take: 100, orderBy: { endedAt: 'desc' } })`
   - For each analysis result JSON, count matches and gaps (result structure has matched_items and gap_items arrays)
   - Aggregate: totalMatches, totalGaps, averageConfidence (average of all match confidences)
   - IMPORTANT: Analysis.result is a Json field -- parse carefully, handle null/undefined (see Pitfall 5)
   - Return `{ totalMatches: number, totalGaps: number, avgConfidence: number }`
  </action>
  <verify>
    <automated>cd C:/Users/ALI/Desktop/ClaudeCodeTest/frontend && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Dashboard data actions return analysis status counts, recent activity, and match/gap aggregates from database</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard UI components</name>
  <files>
    frontend/src/app/(app)/dashboard/page.tsx,
    frontend/src/app/(app)/dashboard/client.tsx,
    frontend/src/app/(app)/dashboard/stat-cards.tsx,
    frontend/src/app/(app)/dashboard/activity-feed.tsx,
    frontend/src/app/(app)/dashboard/statistics-widget.tsx
  </files>
  <action>
1. Update `dashboard/page.tsx` (server component):
   - Import getDashboardStats, getMatchGapStatistics
   - Fetch both in parallel with Promise.all
   - Pass data to `<DashboardClient stats={stats} activity={activity} matchStats={matchStats} />`

2. Create `dashboard/client.tsx` ('use client'):
   - Per user decision: Page header with title "Dashboard" and "Neue Analyse starten" primary Button (Link to /neue-analyse) in top-right (DASH-04)
   - Below header: grid of StatCards
   - Below cards: two-column layout (CSS grid: `grid-cols-1 lg:grid-cols-[2fr_1fr] gap-6`)
   - Left column (wider): ActivityFeed
   - Right column: StatisticsWidget
   - Responsive: On mobile, stack to single column

3. Create `dashboard/stat-cards.tsx` ('use client'):
   - Per user decision: Top row of 3-4 equal-width cards
   - Use shadcn Card component
   - Cards:
     - "Laufende Analysen" -- count with Play/Loader icon (lucide: Activity), blue accent
     - "Abgeschlossene Analysen" -- count with CheckCircle icon, green accent
     - "Fehlerhafte Analysen" -- count with XCircle icon, red accent
     - "Gesamt-Matches" -- totalMatches count with Target icon, FTAG red accent
   - Each card: big number (text-3xl font-bold), label below (text-sm text-muted-foreground)
   - Linear-style: clean, no clutter, icon top-left of card with subtle color
   - Grid: `grid-cols-2 md:grid-cols-4 gap-4`
   - Handle zero state gracefully (show 0, not "No data")

4. Create `dashboard/activity-feed.tsx` ('use client'):
   - Per user decision: Shows all user actions chronologically
   - Render as a Card with header "Letzte Aktivitaeten"
   - Each entry: small avatar circle with initials, user name (bold), action description, relative time ("vor 5 Min.", "vor 1 Std.", "gestern")
   - Action types with icons:
     - analysis_started: Play icon, "hat eine Analyse gestartet"
     - analysis_completed: CheckCircle, "Analyse abgeschlossen"
     - analysis_failed: XCircle, "Analyse fehlgeschlagen"
     - project_created: FolderPlus, "Projekt erstellt"
     - file_uploaded: Upload, "Datei hochgeladen"
     - catalog_updated: Database, "Katalog aktualisiert"
     - user_invited: UserPlus, "Benutzer eingeladen"
     - settings_changed: Settings, "Einstellungen geaendert"
     - Default: Activity icon for unknown actions
   - Empty state: "Noch keine Aktivitaeten"
   - Show max 20 entries, no pagination needed for dashboard

5. Create `dashboard/statistics-widget.tsx` ('use client'):
   - Per user decision: Summary numbers + mini horizontal bar chart
   - Card with header "Statistik"
   - Three metric rows: "Matches: {totalMatches}", "Gaps: {totalGaps}", "Durchschnitts-Konfidenz: {avgConfidence}%"
   - Below metrics: horizontal bar showing match vs gap ratio
     - Green bar (matches) + Red bar (gaps) as proportional widths
     - Labels below: "{matchPercent}% Matches | {gapPercent}% Gaps"
   - If no data: show "Keine Analysedaten vorhanden" with muted text
  </action>
  <verify>
    <automated>cd C:/Users/ALI/Desktop/ClaudeCodeTest/frontend && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Dashboard page shows 4 stat cards with analysis counts, activity feed with user actions and relative times, statistics widget with match/gap bar chart, and prominent "Neue Analyse starten" button</done>
</task>

</tasks>

<verification>
- Dashboard page loads without errors for any authenticated user
- Stat cards show correct counts (including 0 for empty state)
- Activity feed renders entries with icons, names, and relative times
- Statistics widget shows aggregates and proportional bar
- "Neue Analyse starten" button links to /neue-analyse
- All existing tests pass
</verification>

<success_criteria>
- 4 stat cards visible: Laufende, Abgeschlossene, Fehlerhafte Analysen + Gesamt-Matches
- Activity feed shows recent actions with who/what/when
- Statistics widget shows total matches, gaps, avg confidence + bar chart
- "Neue Analyse starten" button prominent in header, navigates to wizard
- Responsive layout: 2-column on desktop, single on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/15-admin-dashboard-polish/15-02-SUMMARY.md`
</output>
