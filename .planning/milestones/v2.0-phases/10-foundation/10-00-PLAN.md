---
phase: 10-foundation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/vitest.config.ts
  - frontend/src/__tests__/infra/database.test.ts
  - frontend/src/__tests__/auth/login.test.ts
  - frontend/src/__tests__/auth/password-reset.test.ts
  - frontend/src/__tests__/hooks/session-timeout.test.ts
  - frontend/src/__tests__/auth/permissions.test.ts
  - frontend/src/__tests__/auth/route-protection.test.ts
  - frontend/src/__tests__/ui/theme.test.ts
  - frontend/src/__tests__/ui/breadcrumbs.test.ts
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "Vitest runs and reports 0 failures on the stub test suite"
    - "All 9 test stub files exist with passing empty describe blocks"
    - "Testing libraries (vitest, testing-library/react, jest-dom, jsdom) are installed"
  artifacts:
    - path: "frontend/vitest.config.ts"
      provides: "Vitest configuration with jsdom environment and path aliases"
    - path: "frontend/src/__tests__/auth/login.test.ts"
      provides: "Stub test file for AUTH-01"
    - path: "frontend/src/__tests__/auth/password-reset.test.ts"
      provides: "Stub test file for AUTH-02"
    - path: "frontend/src/__tests__/hooks/session-timeout.test.ts"
      provides: "Stub test file for AUTH-03"
    - path: "frontend/src/__tests__/auth/permissions.test.ts"
      provides: "Stub test file for AUTH-04"
    - path: "frontend/src/__tests__/auth/route-protection.test.ts"
      provides: "Stub test file for AUTH-05"
    - path: "frontend/src/__tests__/ui/theme.test.ts"
      provides: "Stub test file for UI-01"
    - path: "frontend/src/__tests__/ui/breadcrumbs.test.ts"
      provides: "Stub test file for UI-04"
    - path: "frontend/src/__tests__/infra/database.test.ts"
      provides: "Stub test file for INFRA-01"
  key_links: []
---

<objective>
Install test infrastructure and create all Wave 0 test stub files required by the VALIDATION.md contract.

Purpose: Establish the Vitest test framework so that all subsequent plans can run automated verification commands without "file not found" errors. This is the Nyquist compliance foundation.
Output: Working vitest config and 9 stub test files with empty passing describe blocks.
</objective>

<execution_context>
@C:/Users/ALI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ALI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-foundation/10-CONTEXT.md
@.planning/phases/10-foundation/10-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install test dependencies and create vitest config</name>
  <files>
    frontend/vitest.config.ts
  </files>
  <action>
1. Install test dependencies in the frontend/ directory:
```
cd frontend && npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom
```

2. Create `frontend/vitest.config.ts`:
```typescript
import { defineConfig } from 'vitest/config';
import path from 'path';

export default defineConfig({
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: [],
    include: ['src/__tests__/**/*.test.ts', 'src/__tests__/**/*.test.tsx'],
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
});
```

This config sets jsdom as the default environment (needed for React component tests), enables global test APIs, and maps the `@/` path alias to match the Next.js tsconfig.
  </action>
  <verify>
    <automated>cd C:/Users/ALI/Desktop/ClaudeCodeTest/frontend && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>Vitest is installed and configured, `npx vitest run` executes without config errors</done>
</task>

<task type="auto">
  <name>Task 2: Create all 9 stub test files with passing empty describe blocks</name>
  <files>
    frontend/src/__tests__/infra/database.test.ts,
    frontend/src/__tests__/auth/login.test.ts,
    frontend/src/__tests__/auth/password-reset.test.ts,
    frontend/src/__tests__/hooks/session-timeout.test.ts,
    frontend/src/__tests__/auth/permissions.test.ts,
    frontend/src/__tests__/auth/route-protection.test.ts,
    frontend/src/__tests__/ui/theme.test.ts,
    frontend/src/__tests__/ui/breadcrumbs.test.ts
  </files>
  <action>
Create 9 stub test files, each with a single empty describe block and one placeholder `it.todo()` test. The describe block name should reference the requirement ID for traceability.

Directory structure to create:
```
frontend/src/__tests__/
  auth/
    login.test.ts           # AUTH-01
    password-reset.test.ts  # AUTH-02
    permissions.test.ts     # AUTH-04
    route-protection.test.ts # AUTH-05
  hooks/
    session-timeout.test.ts # AUTH-03
  infra/
    database.test.ts        # INFRA-01
  ui/
    theme.test.ts           # UI-01
    breadcrumbs.test.ts     # UI-04
```

Each file follows this pattern:
```typescript
import { describe, it } from 'vitest';

describe('[REQ-ID] Feature Name', () => {
  it.todo('should [expected behavior] -- stub for Plan XX');
});
```

Specific file contents:

1. `src/__tests__/auth/login.test.ts`:
   - describe: "[AUTH-01] Login flow"
   - todo: "should authenticate with email and password -- stub for Plan 02"

2. `src/__tests__/auth/password-reset.test.ts`:
   - describe: "[AUTH-02] Password reset"
   - todo: "should send 6-digit OTP and allow password reset -- stub for Plan 02"

3. `src/__tests__/hooks/session-timeout.test.ts`:
   - describe: "[AUTH-03] Session timeout"
   - todo: "should warn 5 minutes before session expiry -- stub for Plan 02"

4. `src/__tests__/auth/permissions.test.ts`:
   - describe: "[AUTH-04] Role permissions"
   - todo: "should enforce 4-tier role-based access -- stub for Plan 01"

5. `src/__tests__/auth/route-protection.test.ts`:
   - describe: "[AUTH-05] Route protection"
   - todo: "should redirect unauthenticated users to login -- stub for Plan 02"

6. `src/__tests__/ui/theme.test.ts`:
   - describe: "[UI-01] FTAG design system"
   - todo: "should apply FTAG Rot/Weiss theme tokens -- stub for Plan 01"

7. `src/__tests__/ui/breadcrumbs.test.ts`:
   - describe: "[UI-04] Breadcrumbs"
   - todo: "should render breadcrumb trail from pathname -- stub for Plan 03"

8. `src/__tests__/infra/database.test.ts`:
   - describe: "[INFRA-01] Database setup"
   - todo: "should have Prisma schema with Better Auth models -- stub for Plan 01"

IMPORTANT: All stubs use `it.todo()` (not `it()` with empty body) so they show as pending, not passing or failing.
  </action>
  <verify>
    <automated>cd C:/Users/ALI/Desktop/ClaudeCodeTest/frontend && npx vitest run --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>All 9 test files exist, `npx vitest run` reports 0 failures with 9 todo tests across 8 files</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx vitest run --reporter=verbose` completes with 0 failures
2. All 9 test files exist in the correct directory structure
3. vitest.config.ts has jsdom environment and @ path alias
4. Testing libraries appear in package.json devDependencies
</verification>

<success_criteria>
- Vitest, @testing-library/react, @testing-library/jest-dom, and jsdom are in devDependencies
- vitest.config.ts exists with jsdom environment and path aliases
- All 9 stub test files exist with todo tests matching VALIDATION.md paths exactly
- `npx vitest run` exits with code 0 (no failures)
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation/10-00-SUMMARY.md`
</output>
