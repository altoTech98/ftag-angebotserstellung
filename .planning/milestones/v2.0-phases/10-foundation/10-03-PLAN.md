---
phase: 10-foundation
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - frontend/src/app/(app)/layout.tsx
  - frontend/src/app/(app)/dashboard/page.tsx
  - frontend/src/app/(app)/projekte/page.tsx
  - frontend/src/app/(app)/neue-analyse/page.tsx
  - frontend/src/app/(app)/katalog/page.tsx
  - frontend/src/app/(app)/admin/page.tsx
  - frontend/src/components/layout/sidebar.tsx
  - frontend/src/components/layout/header.tsx
  - frontend/src/components/layout/breadcrumbs.tsx
  - frontend/src/components/layout/user-menu.tsx
  - frontend/src/hooks/use-breadcrumbs.ts
autonomous: true
requirements:
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "Authenticated user sees dark charcoal sidebar with nav items on desktop"
    - "Active nav item has red accent indicator"
    - "Sidebar collapses to icon-only mode on desktop toggle"
    - "Sidebar becomes slide-out drawer on mobile (hamburger menu)"
    - "Tablet shows icon-only sidebar by default"
    - "Breadcrumbs display on all pages in the top header"
    - "User avatar dropdown shows profile and logout options"
    - "All 5 nav items visible: Dashboard, Projekte, Neue Analyse, Katalog, Admin"
  artifacts:
    - path: "frontend/src/components/layout/sidebar.tsx"
      provides: "Collapsible dark sidebar with nav items"
      min_lines: 80
    - path: "frontend/src/components/layout/header.tsx"
      provides: "Top header bar with breadcrumbs and user menu"
    - path: "frontend/src/components/layout/breadcrumbs.tsx"
      provides: "Pathname-based breadcrumb navigation"
    - path: "frontend/src/components/layout/user-menu.tsx"
      provides: "Avatar dropdown with profile/logout"
    - path: "frontend/src/app/(app)/layout.tsx"
      provides: "Authenticated layout with sidebar + header"
    - path: "frontend/src/hooks/use-breadcrumbs.ts"
      provides: "Hook to derive breadcrumb segments from pathname"
      exports: ["useBreadcrumbs"]
  key_links:
    - from: "frontend/src/app/(app)/layout.tsx"
      to: "frontend/src/lib/auth.ts"
      via: "server-side session check"
      pattern: "auth\\.api\\.getSession"
    - from: "frontend/src/components/layout/sidebar.tsx"
      to: "lucide-react"
      via: "icon imports for nav items"
      pattern: "import.*lucide-react"
    - from: "frontend/src/components/layout/user-menu.tsx"
      to: "frontend/src/lib/auth-client.ts"
      via: "signOut call"
      pattern: "signOut"
    - from: "frontend/src/hooks/use-breadcrumbs.ts"
      to: "next/navigation"
      via: "usePathname"
      pattern: "usePathname"
---

<objective>
Build the authenticated app shell: dark sidebar navigation, top header with breadcrumbs and user menu, responsive layout, and placeholder pages.

Purpose: Users see the FTAG-branded layout with sidebar navigation, breadcrumbs, and responsive behavior across desktop/tablet/mobile.
Output: Complete app layout shell with all navigation items and placeholder page content.
</objective>

<execution_context>
@C:/Users/ALI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ALI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-foundation/10-CONTEXT.md
@.planning/phases/10-foundation/10-RESEARCH.md
@.planning/phases/10-foundation/10-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/lib/auth.ts:
```typescript
export const auth = betterAuth({...});
// auth.api.getSession({ headers }) -> { user: { id, name, email, role, ... }, session: { ... } }
// auth.api.userHasPermission({ body: { userId, permissions } }) -> { data: { success: boolean } }
```

From src/lib/auth-client.ts:
```typescript
export const { signIn, signUp, signOut, useSession } = authClient;
```

From src/styles/globals.css:
```css
/* Available color utilities: bg-sidebar-bg, text-sidebar-text, bg-sidebar-active, bg-sidebar-hover,
   bg-primary, text-primary, bg-ftag-red, bg-ftag-red-hover, bg-ftag-red-light,
   bg-muted, text-muted-foreground, border-border */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build sidebar navigation and header with breadcrumbs</name>
  <files>
    frontend/src/components/layout/sidebar.tsx,
    frontend/src/components/layout/header.tsx,
    frontend/src/components/layout/breadcrumbs.tsx,
    frontend/src/components/layout/user-menu.tsx,
    frontend/src/hooks/use-breadcrumbs.ts
  </files>
  <action>
**Sidebar Component:**
1. Create `src/components/layout/sidebar.tsx` -- client component ("use client"):
   - Dark charcoal background (bg-sidebar-bg), white text (text-sidebar-text)
   - FTAG logo/name at top: "FTAG" text in white, small subtitle "Angebotserstellung"
   - Navigation items with lucide-react icons:
     - Dashboard (LayoutDashboard icon) -> /dashboard
     - Projekte (FolderOpen icon) -> /projekte
     - Neue Analyse (PlusCircle icon) -> /neue-analyse
     - Katalog (BookOpen icon) -> /katalog
     - Admin (Settings icon) -> /admin
   - ALL nav items visible to ALL roles (per CONTEXT.md -- denied pages show permission message, items never hidden)
   - Active item: red left border (3px) + red-tinted background (bg-sidebar-active/10 or similar) per CONTEXT.md "red accent for active item"
   - Hover state: slightly lighter background (bg-sidebar-hover)
   - Collapsed state: show only icons (icon-only mode), toggle via chevron button at bottom
   - Store collapsed state in localStorage for persistence
   - Smooth transition animation on collapse/expand (width transition, ~200ms)
   - Linear/Notion-style: clean, minimal, professional feel

   Responsive behavior (per CONTEXT.md):
   - Desktop (lg+): sidebar expanded by default, collapsible via toggle button
   - Tablet (md): sidebar collapsed to icons by default
   - Mobile (< md): sidebar hidden, opened via hamburger menu as slide-out overlay drawer
   - Mobile drawer: overlay backdrop + slide-in from left, close on backdrop click or X button

   Props: `user: { name: string, email: string, role: string }`

**Header Component:**
2. Create `src/components/layout/header.tsx`:
   - Sticky top bar (h-14 or h-16)
   - Left side: hamburger menu button (mobile only, hidden on md+) + Breadcrumbs component
   - Right side: notification bell icon (placeholder, empty state, greyed out with tooltip "Kommt bald") + UserMenu component
   - Subtle bottom border (border-b border-border)
   - Clean white background

**Breadcrumbs Component:**
3. Create `src/hooks/use-breadcrumbs.ts` -- client hook:
   - Uses `usePathname()` from next/navigation
   - Splits pathname into segments
   - Maps segments to German labels: dashboard->"Dashboard", projekte->"Projekte", neue-analyse->"Neue Analyse", katalog->"Katalog", admin->"Admin"
   - Returns array of { label: string, href: string } breadcrumb items
   - Always starts with "Start" (href: "/dashboard") as root

4. Create `src/components/layout/breadcrumbs.tsx` -- client component:
   - Uses useBreadcrumbs hook
   - Renders breadcrumb trail with separator (ChevronRight icon, size 14, text-muted-foreground)
   - Last item is not a link (current page)
   - Other items are Next.js Link components
   - Use shadcn Breadcrumb component if available, otherwise build with simple nav/ol

**User Menu Component:**
5. Create `src/components/layout/user-menu.tsx` -- client component:
   - Avatar circle with user initials (first letter of name, bg-primary text-primary-foreground)
   - Click opens dropdown (use shadcn DropdownMenu or Popover):
     - User name and email at top (non-clickable, muted text)
     - Divider
     - "Profil" item (placeholder, links to # for now)
     - "Abmelden" item (calls signOut from auth-client.ts, redirects to /login)
   - Dropdown positioned bottom-right
  </action>
  <verify>
    <automated>cd C:/Users/ALI/Desktop/ClaudeCodeTest/frontend && npx next build 2>&1 | tail -10</automated>
  </verify>
  <done>Sidebar renders with 5 nav items, red active indicator, collapse toggle, and responsive mobile drawer. Header shows breadcrumbs and user menu. All text in German.</done>
</task>

<task type="auto">
  <name>Task 2: Create authenticated layout and placeholder pages with role gating</name>
  <files>
    frontend/src/app/(app)/layout.tsx,
    frontend/src/app/(app)/dashboard/page.tsx,
    frontend/src/app/(app)/projekte/page.tsx,
    frontend/src/app/(app)/neue-analyse/page.tsx,
    frontend/src/app/(app)/katalog/page.tsx,
    frontend/src/app/(app)/admin/page.tsx
  </files>
  <action>
**Authenticated Layout:**
1. Create `src/app/(app)/layout.tsx` -- server component:
   - Check session via `auth.api.getSession({ headers: await headers() })` as in RESEARCH.md Code Examples
   - If no session: `redirect("/login")`
   - Render layout structure: flex container (h-screen)
     - Sidebar component (pass user data from session)
     - Main content area (flex-1 overflow-auto)
       - Header component (pass user data)
       - Content area with padding (p-6) for {children}
   - NOTE: SessionWarningModal from Plan 02 will be added here in Plan 04 (integration)

**Placeholder Pages:**
Each page is a server component that checks the user's role and shows content or "Keine Berechtigung" message.

2. Create `src/app/(app)/dashboard/page.tsx`:
   - All roles can access (viewer+)
   - Show: heading "Dashboard", subtitle "Willkommen bei FTAG Angebotserstellung", placeholder card "Dashboard wird in Phase 15 implementiert"
   - Clean, minimal layout with proper spacing

3. Create `src/app/(app)/projekte/page.tsx`:
   - Viewer+ can view (all roles)
   - Show: heading "Projekte", placeholder "Projektverwaltung wird in Phase 12 implementiert"

4. Create `src/app/(app)/neue-analyse/page.tsx`:
   - Check permission: analysis.create (analyst+ role)
   - If no permission: show "Keine Berechtigung" skeleton (per CONTEXT.md -- show page skeleton with message and note to contact Admin)
   - If permitted: heading "Neue Analyse", placeholder "Analyse-Wizard wird in Phase 13 implementiert"

5. Create `src/app/(app)/katalog/page.tsx`:
   - Viewer+ can view, but catalog.upload requires manager+
   - Show: heading "Katalog", placeholder "Katalogverwaltung wird in Phase 14 implementiert"

6. Create `src/app/(app)/admin/page.tsx`:
   - Check permission: admin.access (admin role only)
   - If no permission: show "Keine Berechtigung" message exactly as in RESEARCH.md Code Examples (centered, with "Bitte kontaktieren Sie Ihren Administrator" note)
   - If permitted: heading "Administration", placeholder "Admin-Bereich wird in Phase 15 implementiert"

   Role checking pattern for each page:
   ```typescript
   const session = await auth.api.getSession({ headers: await headers() });
   const hasAccess = await auth.api.userHasPermission({
     body: { userId: session!.user.id, permissions: { resource: ["action"] } },
   });
   if (!hasAccess.data?.success) { /* show Keine Berechtigung */ }
   ```

   "Keine Berechtigung" component: Create a reusable component or inline JSX:
   - Centered vertically and horizontally (min-h-[60vh])
   - Icon: ShieldX or Lock from lucide-react
   - Heading: "Keine Berechtigung" (text-2xl font-semibold)
   - Body: "Sie haben keine Berechtigung fuer diese Seite. Bitte kontaktieren Sie Ihren Administrator."
   - Muted text color for body
  </action>
  <verify>
    <automated>cd C:/Users/ALI/Desktop/ClaudeCodeTest/frontend && npx next build 2>&1 | tail -10</automated>
  </verify>
  <done>Authenticated layout renders sidebar + header + content area, all 5 placeholder pages exist with proper role checks, admin page shows "Keine Berechtigung" for non-admin users, all text in German</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx next build` completes without errors
2. (app)/layout.tsx checks session and redirects to /login if missing
3. Sidebar has 5 nav items with lucide icons and red active indicator
4. Sidebar collapses to icon-only mode
5. Breadcrumbs show correct path segments in German
6. Admin page returns "Keine Berechtigung" for non-admin roles
7. All pages render with FTAG design tokens
</verification>

<success_criteria>
- Dark charcoal sidebar with 5 nav items and red active accent
- Sidebar is collapsible on desktop, icon-only on tablet, drawer on mobile
- Header shows breadcrumbs on left, notification placeholder + user menu on right
- User menu dropdown has profile placeholder and logout button
- All 5 placeholder pages exist with role-appropriate access checks
- "Keine Berechtigung" message shown on unauthorized access (not hidden nav items)
- All UI text is in German
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation/10-03-SUMMARY.md`
</output>
